--- a/net/minecraft/server/commands/RaidCommand.java
+++ b/net/minecraft/server/commands/RaidCommand.java
@@ -3,6 +_,7 @@
 import com.mojang.brigadier.CommandDispatcher;
 import com.mojang.brigadier.arguments.IntegerArgumentType;
 import com.mojang.brigadier.exceptions.CommandSyntaxException;
+import java.util.List;
 import net.minecraft.commands.CommandBuildContext;
 import net.minecraft.commands.CommandSourceStack;
 import net.minecraft.commands.Commands;
@@ -59,10 +_,18 @@
     }
 
     private static int glow(CommandSourceStack source) throws CommandSyntaxException {
-        Raid raid = getRaid(source.getPlayerOrException());
-        if (raid != null) {
-            for (Raider raider : raid.getAllRaiders()) {
-                raider.addEffect(new MobEffectInstance(MobEffects.GLOWING, 1000, 1));
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> glow(source))) {
+            return 0;
+        }
+
+        ServerPlayer player = source.getPlayerOrException();
+        List<Raider> raiders = callOnPlayerRaid(source, player, raid -> List.copyOf(raid.getAllRaiders()));
+        if (raiders != null) {
+            for (Raider raider : raiders) {
+                RegionizedCommandHelper.callOnEntityRegion(source, raider, () -> {
+                    raider.addEffect(new MobEffectInstance(MobEffects.GLOWING, 1000, 1));
+                    return null;
+                });
             }
         }
 
@@ -70,15 +_,26 @@
     }
 
     private static int setRaidOmenLevel(CommandSourceStack source, int level) throws CommandSyntaxException {
-        Raid raid = getRaid(source.getPlayerOrException());
-        if (raid != null) {
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> setRaidOmenLevel(source, level))) {
+            return 0;
+        }
+
+        ServerPlayer player = source.getPlayerOrException();
+        RaidOmenUpdate result = callOnPlayerRaid(source, player, raid -> {
             int max = raid.getMaxRaidOmenLevel();
             if (level > max) {
-                source.sendFailure(Component.literal("Sorry, the max raid omen level you can set is " + max));
+                return new RaidOmenUpdate(true, 0, max);
+            }
+
+            int before = raid.getRaidOmenLevel();
+            raid.setRaidOmenLevel(level);
+            return new RaidOmenUpdate(false, before, max);
+        });
+        if (result != null) {
+            if (result.tooHigh()) {
+                source.sendFailure(Component.literal("Sorry, the max raid omen level you can set is " + result.max()));
             } else {
-                int before = raid.getRaidOmenLevel();
-                raid.setRaidOmenLevel(level);
-                source.sendSuccess(() -> Component.literal("Changed village's raid omen level from " + before + " to " + level), false);
+                source.sendSuccess(() -> Component.literal("Changed village's raid omen level from " + result.before() + " to " + level), false);
             }
         } else {
             source.sendFailure(Component.literal("No raid found here"));
@@ -88,60 +_,100 @@
     }
 
     private static int spawnLeader(CommandSourceStack source) {
-        source.sendSuccess(() -> Component.literal("Spawned a raid captain"), false);
-        Raider raider = EntityType.PILLAGER.create(source.getLevel(), EntitySpawnReason.COMMAND);
-        if (raider == null) {
-            source.sendFailure(Component.literal("Pillager failed to spawn"));
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> spawnLeader(source))) {
             return 0;
-        } else {
+        }
+
+        BlockPos spawnPos = BlockPos.containing(source.getPosition());
+        Boolean spawned = RegionizedCommandHelper.callOnRegion(source, source.getLevel(), spawnPos, () -> {
+            Raider raider = EntityType.PILLAGER.create(source.getLevel(), EntitySpawnReason.COMMAND);
+            if (raider == null) {
+                return false;
+            }
+
             raider.setPatrolLeader(true);
             raider.setItemSlot(EquipmentSlot.HEAD, Raid.getOminousBannerInstance(source.registryAccess().lookupOrThrow(Registries.BANNER_PATTERN)));
             raider.setPos(source.getPosition().x, source.getPosition().y, source.getPosition().z);
             raider.finalizeSpawn(
-                source.getLevel(), source.getLevel().getCurrentDifficultyAt(BlockPos.containing(source.getPosition())), EntitySpawnReason.COMMAND, null
+                source.getLevel(), source.getLevel().getCurrentDifficultyAt(spawnPos), EntitySpawnReason.COMMAND, null
             );
             source.getLevel().addFreshEntityWithPassengers(raider);
-            return 1;
+            return true;
+        });
+        if (!spawned) {
+            source.sendFailure(Component.literal("Pillager failed to spawn"));
+            return 0;
         }
+
+        source.sendSuccess(() -> Component.literal("Spawned a raid captain"), false);
+        return 1;
     }
 
     private static int playSound(CommandSourceStack source, @Nullable Component type) {
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> playSound(source, type))) {
+            return 0;
+        }
+
         if (type != null && type.getString().equals("local")) {
             ServerLevel level = source.getLevel();
             Vec3 pos = source.getPosition().add(5.0, 0.0, 0.0);
-            level.playSeededSound(null, pos.x, pos.y, pos.z, SoundEvents.RAID_HORN, SoundSource.NEUTRAL, 2.0F, 1.0F, level.getRandom().nextLong());
+            BlockPos blockPos = BlockPos.containing(pos);
+            RegionizedCommandHelper.callOnRegion(source, level, blockPos, () -> {
+                level.playSeededSound(null, pos.x, pos.y, pos.z, SoundEvents.RAID_HORN, SoundSource.NEUTRAL, 2.0F, 1.0F, level.getRandom().nextLong());
+                return null;
+            });
         }
 
         return 1;
     }
 
     private static int start(CommandSourceStack source, int raidOmenLevel) throws CommandSyntaxException {
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> start(source, raidOmenLevel))) {
+            return 0;
+        }
+
         ServerPlayer player = source.getPlayerOrException();
-        BlockPos pos = player.blockPosition();
-        if (player.level().isRaided(pos)) {
-            source.sendFailure(Component.literal("Raid already started close by"));
-            return -1;
-        } else {
+        StartRaidResult result = RegionizedCommandHelper.callOnPlayerRegion(source, player, () -> {
+            BlockPos pos = player.blockPosition();
+            if (player.level().isRaided(pos)) {
+                return StartRaidResult.ALREADY_STARTED;
+            }
+
             Raids raids = player.level().getRaids();
-            Raid raid = raids.createOrExtendRaid(player, player.blockPosition());
-            if (raid != null) {
-                raid.setRaidOmenLevel(raidOmenLevel);
-                raids.setDirty();
-                source.sendSuccess(() -> Component.literal("Created a raid in your local village"), false);
-            } else {
-                source.sendFailure(Component.literal("Failed to create a raid in your local village"));
+            Raid raid = raids.createOrExtendRaid(player, pos);
+            if (raid == null) {
+                return StartRaidResult.FAILED;
             }
 
-            return 1;
-        }
+            raid.setRaidOmenLevel(raidOmenLevel);
+            raids.setDirty();
+            return StartRaidResult.CREATED;
+        });
+        if (result == StartRaidResult.ALREADY_STARTED) {
+            source.sendFailure(Component.literal("Raid already started close by"));
+            return -1;
+        }
+
+        if (result == StartRaidResult.CREATED) {
+            source.sendSuccess(() -> Component.literal("Created a raid in your local village"), false);
+        } else {
+            source.sendFailure(Component.literal("Failed to create a raid in your local village"));
+        }
+
+        return 1;
     }
 
     private static int stop(CommandSourceStack source) throws CommandSyntaxException {
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> stop(source))) {
+            return 0;
+        }
+
         ServerPlayer player = source.getPlayerOrException();
-        BlockPos pos = player.blockPosition();
-        Raid raid = player.level().getRaidAt(pos);
-        if (raid != null) {
+        Boolean stopped = callOnPlayerRaid(source, player, raid -> {
             raid.stop();
+            return true;
+        });
+        if (stopped != null) {
             source.sendSuccess(() -> Component.literal("Stopped raid"), false);
             return 1;
         } else {
@@ -151,22 +_,37 @@
     }
 
     private static int check(CommandSourceStack source) throws CommandSyntaxException {
-        Raid raid = getRaid(source.getPlayerOrException());
-        if (raid != null) {
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> check(source))) {
+            return 0;
+        }
+
+        ServerPlayer player = source.getPlayerOrException();
+        RaidSnapshot snapshot = callOnPlayerRaid(
+            source,
+            player,
+            raid -> new RaidSnapshot(
+                raid.getGroupsSpawned(),
+                raid.getRaidOmenLevel(),
+                raid.getTotalRaidersAlive(),
+                raid.getHealthOfLivingRaiders(),
+                raid.getTotalHealth()
+            )
+        );
+        if (snapshot != null) {
             StringBuilder status = new StringBuilder();
             status.append("Found a started raid! ");
             source.sendSuccess(() -> Component.literal(status.toString()), false);
             StringBuilder status2 = new StringBuilder();
             status2.append("Num groups spawned: ");
-            status2.append(raid.getGroupsSpawned());
+            status2.append(snapshot.groupsSpawned());
             status2.append(" Raid omen level: ");
-            status2.append(raid.getRaidOmenLevel());
+            status2.append(snapshot.raidOmenLevel());
             status2.append(" Num mobs: ");
-            status2.append(raid.getTotalRaidersAlive());
+            status2.append(snapshot.totalRaidersAlive());
             status2.append(" Raid health: ");
-            status2.append(raid.getHealthOfLivingRaiders());
+            status2.append(snapshot.healthOfLivingRaiders());
             status2.append(" / ");
-            status2.append(raid.getTotalHealth());
+            status2.append(snapshot.totalHealth());
             source.sendSuccess(() -> Component.literal(status2.toString()), false);
             return 1;
         } else {
@@ -175,7 +_,27 @@
         }
     }
 
-    private static @Nullable Raid getRaid(ServerPlayer player) {
-        return player.level().getRaidAt(player.blockPosition());
+    private static <T> @Nullable T callOnPlayerRaid(CommandSourceStack source, ServerPlayer player, RaidAction<T> action) {
+        return RegionizedCommandHelper.callOnPlayerRegion(source, player, () -> {
+            Raid raid = player.level().getRaidAt(player.blockPosition());
+            return raid == null ? null : action.apply(raid);
+        });
+    }
+
+    private enum StartRaidResult {
+        CREATED,
+        ALREADY_STARTED,
+        FAILED
+    }
+
+    private record RaidOmenUpdate(boolean tooHigh, int before, int max) {
+    }
+
+    private record RaidSnapshot(int groupsSpawned, int raidOmenLevel, int totalRaidersAlive, float healthOfLivingRaiders, float totalHealth) {
+    }
+
+    @FunctionalInterface
+    private interface RaidAction<T> {
+        T apply(Raid raid);
     }
 }
