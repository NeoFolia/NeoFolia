--- a/net/minecraft/server/commands/FetchProfileCommand.java
+++ b/net/minecraft/server/commands/FetchProfileCommand.java
@@ -3,6 +_,7 @@
 import com.mojang.authlib.GameProfile;
 import com.mojang.brigadier.CommandDispatcher;
 import com.mojang.brigadier.arguments.StringArgumentType;
+import com.mojang.brigadier.context.CommandContext;
 import com.mojang.brigadier.exceptions.CommandSyntaxException;
 import com.mojang.brigadier.exceptions.DynamicCommandExceptionType;
 import java.util.List;
@@ -52,7 +_,7 @@
                     Commands.literal("entity")
                         .then(
                             Commands.argument("entity", EntityArgument.entity())
-                                .executes(c -> printForEntity(c.getSource(), EntityArgument.getEntity(c, "entity")))
+                                .executes(FetchProfileCommand::printForEntity)
                         )
                 )
         );
@@ -124,7 +_,8 @@
                 () -> {
                     Component nameComponent = Component.literal(name);
                     Optional<GameProfile> result = resolver.fetchByName(name);
-                    server.execute(
+                    RegionizedCommandHelper.executeGlobalOrServer(
+                        source,
                         () -> result.ifPresentOrElse(
                             profile -> reportResolvedProfile(source, profile, "commands.fetchprofile.name.success", nameComponent),
                             () -> source.sendFailure(Component.translatable("commands.fetchprofile.name.failure", nameComponent))
@@ -143,7 +_,8 @@
                 () -> {
                     Component idComponent = Component.translationArg(id);
                     Optional<GameProfile> result = resolver.fetchById(id);
-                    server.execute(
+                    RegionizedCommandHelper.executeGlobalOrServer(
+                        source,
                         () -> result.ifPresentOrElse(
                             profile -> reportResolvedProfile(source, profile, "commands.fetchprofile.id.success", idComponent),
                             () -> source.sendFailure(Component.translatable("commands.fetchprofile.id.failure", idComponent))
@@ -154,16 +_,34 @@
         return 1;
     }
 
+    private static int printForEntity(CommandContext<CommandSourceStack> context) throws CommandSyntaxException {
+        CommandSourceStack source = context.getSource();
+        if (RegionizedCommandHelper.scheduleGlobalIfRegionized(source, () -> printForEntity(context))) {
+            return 0;
+        }
+
+        return printForEntity(source, EntityArgument.getEntity(context, "entity"));
+    }
+
     private static int printForEntity(CommandSourceStack source, Entity entity) throws CommandSyntaxException {
         if (entity instanceof Avatar avatar) {
             printForAvatar(source, avatar);
             return 1;
         } else {
-            throw NO_PROFILE.create(entity.getDisplayName());
+            Component entityName = RegionizedCommandHelper.callOnEntityRegion(source, entity, entity::getDisplayName);
+            throw NO_PROFILE.create(entityName);
         }
     }
 
     public static void printForAvatar(CommandSourceStack source, Avatar avatar) {
-        reportResolvedProfile(source, "commands.fetchprofile.entity.success", avatar.getDisplayName(), avatar.getProfile());
+        AvatarProfileResult result = RegionizedCommandHelper.callOnEntityRegion(
+            source,
+            avatar,
+            () -> new AvatarProfileResult(avatar.getDisplayName(), avatar.getProfile())
+        );
+        reportResolvedProfile(source, "commands.fetchprofile.entity.success", result.name(), result.profile());
+    }
+
+    private record AvatarProfileResult(Component name, ResolvableProfile profile) {
     }
 }
