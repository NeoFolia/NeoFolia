--- a/net/minecraft/server/jsonrpc/internalapi/MinecraftGameRuleServiceImpl.java
+++ b/net/minecraft/server/jsonrpc/internalapi/MinecraftGameRuleServiceImpl.java
@@ -21,6 +_,13 @@
 
     @Override
     public <T> GameRulesService.GameRuleUpdate<T> updateGameRule(GameRulesService.GameRuleUpdate<T> update, ClientInfo clientInfo) {
+        if (!this.server.isSameThread() && !net.neoforged.neoforge.server.region.RegionizedTickContext.isRegionThread()) {
+            return RegionizedJsonRpcGuard.callOnServerThread(this.server, () -> this.updateGameRule(update, clientInfo));
+        }
+        if (RegionizedJsonRpcGuard.scheduleGlobalIfRegionized(this.server, () -> this.updateGameRule(update, clientInfo))) {
+            return update;
+        }
+
         GameRule<T> gameRule = update.gameRule();
         T oldValue = this.gameRules.get(gameRule);
         T newValue = update.value();
@@ -37,11 +_,11 @@
 
     @Override
     public Stream<GameRule<?>> getAvailableGameRules() {
-        return this.gameRules.availableRules();
+        return RegionizedJsonRpcGuard.callOnServerThread(this.server, this.gameRules::availableRules);
     }
 
     @Override
     public <T> T getRuleValue(GameRule<T> gameRule) {
-        return this.gameRules.get(gameRule);
+        return RegionizedJsonRpcGuard.callOnServerThread(this.server, () -> this.gameRules.get(gameRule));
     }
 }
