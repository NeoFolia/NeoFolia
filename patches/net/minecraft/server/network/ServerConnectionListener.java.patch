--- a/net/minecraft/server/network/ServerConnectionListener.java
+++ b/net/minecraft/server/network/ServerConnectionListener.java
@@ -23,11 +_,14 @@
 import java.util.Iterator;
 import java.util.List;
 import java.util.Objects;
+import java.util.Queue;
+import java.util.concurrent.ConcurrentLinkedQueue;
 import java.util.concurrent.TimeUnit;
 import net.minecraft.CrashReport;
 import net.minecraft.ReportedException;
 import net.minecraft.SharedConstants;
 import net.minecraft.network.Connection;
+import net.minecraft.network.PacketListener;
 import net.minecraft.network.PacketSendListener;
 import net.minecraft.network.RateKickingConnection;
 import net.minecraft.network.chat.Component;
@@ -39,17 +_,21 @@
 
 public class ServerConnectionListener {
     private static final Logger LOGGER = LogUtils.getLogger();
+    private static final int READ_TIMEOUT = Integer.parseInt(System.getProperty("neoforge.readTimeout", "30"));
     private final MinecraftServer server;
     public volatile boolean running;
     private final List<ChannelFuture> channels = Collections.synchronizedList(Lists.newArrayList());
     private final List<Connection> connections = Collections.synchronizedList(Lists.newArrayList());
+    private final List<Connection> connectionsToTick = Collections.synchronizedList(Lists.newArrayList());
 
     public ServerConnectionListener(MinecraftServer server) {
         this.server = server;
         this.running = true;
     }
 
-    public void startTcpServerListener(@Nullable InetAddress address, int port) throws IOException {
+    public synchronized void startTcpServerListener(@Nullable InetAddress address, int port) throws IOException {
+        if (address == null) address = new java.net.InetSocketAddress(port).getAddress();
+        net.neoforged.neoforge.network.DualStackUtils.checkIPv6(address);
         synchronized (this.channels) {
             EventLoopGroupHolder eventLoopGroupHolder = EventLoopGroupHolder.remote(this.server.useNativeTransport());
             this.channels
@@ -69,7 +_,7 @@
                                     } catch (ChannelException var5) {
                                     }
 
-                                    ChannelPipeline pipeline = channel.pipeline().addLast("timeout", new ReadTimeoutHandler(30));
+                                    ChannelPipeline pipeline = channel.pipeline().addLast("timeout", new ReadTimeoutHandler(READ_TIMEOUT));
                                     if (ServerConnectionListener.this.server.repliesToStatus()) {
                                         pipeline.addLast("legacy_query", new LegacyQueryHandler(ServerConnectionListener.this.getServer()));
                                     }
@@ -80,6 +_,7 @@
                                         ? new RateKickingConnection(rateLimitPacketsPerSecond)
                                         : new Connection(PacketFlow.SERVERBOUND));
                                     ServerConnectionListener.this.connections.add(connection);
+                                    ServerConnectionListener.this.connectionsToTick.add(connection);
                                     connection.configurePacketHandler(pipeline);
                                     connection.setListenerForServerboundHandshake(
                                         new ServerHandshakePacketListenerImpl(ServerConnectionListener.this.server, connection)
@@ -87,7 +_,7 @@
                                 }
                             }
                         )
-                        .group(eventLoopGroupHolder.eventLoopGroup())
+                        .group(eventLoopGroupHolder.serverEventLoopGroup())
                         .localAddress(address, port)
                         .bind()
                         .syncUninterruptibly()
@@ -95,7 +_,7 @@
         }
     }
 
-    public SocketAddress startMemoryChannel() {
+    public synchronized SocketAddress startMemoryChannel() {
         ChannelFuture newChannel;
         synchronized (this.channels) {
             newChannel = new ServerBootstrap()
@@ -113,6 +_,7 @@
                                 new MemoryServerHandshakePacketListenerImpl(ServerConnectionListener.this.server, connection)
                             );
                             ServerConnectionListener.this.connections.add(connection);
+                            ServerConnectionListener.this.connectionsToTick.add(connection);
                             ChannelPipeline pipeline = channel.pipeline();
                             Connection.configureInMemoryPipeline(pipeline, PacketFlow.SERVERBOUND);
                             if (SharedConstants.DEBUG_FAKE_LATENCY_MS > 0) {
@@ -126,7 +_,7 @@
                         }
                     }
                 )
-                .group(EventLoopGroupHolder.local().eventLoopGroup())
+                .group(EventLoopGroupHolder.local().serverEventLoopGroup())
                 .localAddress(LocalAddress.ANY)
                 .bind()
                 .syncUninterruptibly();
@@ -136,43 +_,98 @@
         return newChannel.channel().localAddress();
     }
 
-    public void stop() {
+    public synchronized void stop() {
         this.running = false;
 
-        for (ChannelFuture channel : this.channels) {
-            try {
-                channel.channel().close().sync();
-            } catch (InterruptedException var4) {
-                LOGGER.error("Interrupted whilst closing channel");
+        synchronized (this.channels) {
+            for (ChannelFuture channel : this.channels) {
+                try {
+                    channel.channel().close().sync();
+                } catch (InterruptedException var4) {
+                    LOGGER.error("Interrupted whilst closing channel");
+                }
             }
         }
     }
 
     public void tick() {
-        synchronized (this.connections) {
-            Iterator<Connection> iterator = this.connections.iterator();
-
-            while (iterator.hasNext()) {
-                Connection connection = iterator.next();
-                if (!connection.isConnecting()) {
-                    if (connection.isConnected()) {
-                        try {
-                            connection.tick();
-                        } catch (Exception var7) {
-                            if (connection.isMemoryConnection()) {
-                                throw new ReportedException(CrashReport.forThrowable(var7, "Ticking memory connection"));
-                            }
-
-                            LOGGER.warn("Failed to handle packet for {}", connection.getLoggableAddress(this.server.logIPs()), var7);
-                            Component component = Component.literal("Internal server error");
-                            connection.send(new ClientboundDisconnectPacket(component), PacketSendListener.thenRun(() -> connection.disconnect(component)));
-                            connection.setReadOnly();
-                        }
-                    } else {
-                        iterator.remove();
-                        connection.handleDisconnection();
-                    }
-                }
+        List<Connection> connectionSnapshot;
+        synchronized (this.connectionsToTick) {
+            connectionSnapshot = List.copyOf(this.connectionsToTick);
+        }
+
+        for (Connection connection : connectionSnapshot) {
+            if (!connection.isConnecting()) {
+                if (!connection.isConnected()) {
+                    this.removeConnection(connection);
+                    continue;
+                }
+
+                if (this.scheduleGameConnectionTickOnRegion(connection)) {
+                    continue;
+                }
+
+                this.tickConnection(connection);
+            }
+        }
+    }
+
+    private boolean scheduleGameConnectionTickOnRegion(Connection connection) {
+        if (!this.server.regionizedTaskDispatcher().isRunning()) {
+            return false;
+        }
+
+        PacketListener packetListener = connection.getPacketListener();
+        return packetListener instanceof ServerGamePacketListenerImpl gamePacketListener
+            && !gamePacketListener.isWaitingForSwitchToConfig()
+            && gamePacketListener.ensureRegionizedConnectionTicker(() -> this.tickConnection(connection));
+    }
+
+    private void tickConnection(Connection connection) {
+        try {
+            connection.tick();
+        } catch (Exception exception) {
+            if (connection.isMemoryConnection()) {
+                throw new ReportedException(CrashReport.forThrowable(exception, "Ticking memory connection"));
+            }
+
+            LOGGER.warn("Failed to handle packet for {}", connection.getLoggableAddress(this.server.logIPs()), exception);
+            Component component = Component.literal("Internal server error");
+            connection.send(new ClientboundDisconnectPacket(component), PacketSendListener.thenRun(() -> connection.disconnect(component)));
+            connection.setReadOnly();
+        }
+
+        if (!connection.isConnected()) {
+            this.removeConnection(connection);
+        }
+    }
+
+    private void removeConnection(Connection connection) {
+        synchronized (this.connections) {
+            this.connections.remove(connection);
+        }
+        synchronized (this.connectionsToTick) {
+            this.connectionsToTick.remove(connection);
+        }
+        connection.handleDisconnection();
+    }
+
+    public void markConnectionRegionized(Connection connection) {
+        synchronized (this.connectionsToTick) {
+            this.connectionsToTick.remove(connection);
+        }
+    }
+
+    public void markConnectionGlobalTick(Connection connection) {
+        synchronized (this.connections) {
+            if (!this.connections.contains(connection)) {
+                return;
+            }
+        }
+
+        synchronized (this.connectionsToTick) {
+            if (!this.connectionsToTick.contains(connection)) {
+                this.connectionsToTick.add(connection);
             }
         }
     }
@@ -182,14 +_,16 @@
     }
 
     public List<Connection> getConnections() {
-        return this.connections;
+        synchronized (this.connections) {
+            return List.copyOf(this.connections);
+        }
     }
 
     private static class LatencySimulator extends ChannelInboundHandlerAdapter {
         private static final Timer TIMER = new HashedWheelTimer();
         private final int delay;
         private final int jitter;
-        private final List<ServerConnectionListener.LatencySimulator.DelayedMessage> queuedMessages = Lists.newArrayList();
+        private final Queue<ServerConnectionListener.LatencySimulator.DelayedMessage> queuedMessages = new ConcurrentLinkedQueue<>();
 
         public LatencySimulator(int delay, int jitter) {
             this.delay = delay;
@@ -208,8 +_,10 @@
         }
 
         private void onTimeout(Timeout timeout) {
-            ServerConnectionListener.LatencySimulator.DelayedMessage next = this.queuedMessages.remove(0);
-            next.ctx.fireChannelRead(next.msg);
+            ServerConnectionListener.LatencySimulator.DelayedMessage next = this.queuedMessages.poll();
+            if (next != null) {
+                next.ctx.fireChannelRead(next.msg);
+            }
         }
 
         private static class DelayedMessage {
