--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -22,6 +_,7 @@
 import java.io.IOException;
 import java.io.Writer;
 import java.nio.file.Path;
+import java.util.AbstractMap;
 import java.util.ArrayList;
 import java.util.HashMap;
 import java.util.List;
@@ -104,6 +_,9 @@
 import net.minecraft.world.level.storage.LevelStorageSource;
 import net.minecraft.world.level.storage.SavedDataStorage;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionKey;
+import net.neoforged.neoforge.server.region.RegionizedTickContext;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.apache.commons.lang3.mutable.MutableBoolean;
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
@@ -149,7 +_,7 @@
     private final LongSet chunksToEagerlySave = new LongLinkedOpenHashSet();
     private final Queue<Runnable> unloadQueue = Queues.newConcurrentLinkedQueue();
     private final AtomicInteger activeChunkWrites = new AtomicInteger();
-    private int serverViewDistance;
+    private volatile int serverViewDistance;
     private final WorldGenContext worldGenContext;
 
     public ChunkMap(
@@ -209,7 +_,9 @@
     }
 
     private void setChunkUnsaved(ChunkPos chunkPos) {
-        this.chunksToEagerlySave.add(chunkPos.pack());
+        synchronized (this.chunksToEagerlySave) {
+            this.chunksToEagerlySave.add(chunkPos.pack());
+        }
     }
 
     protected ChunkGenerator generator() {
@@ -248,7 +_,7 @@
         return this.lightEngine;
     }
 
-    public @Nullable ChunkHolder getUpdatingChunkIfPresent(long key) {
+    public synchronized @Nullable ChunkHolder getUpdatingChunkIfPresent(long key) {
         return this.updatingChunkMap.get(key);
     }
 
@@ -256,6 +_,78 @@
         return this.visibleChunkMap.get(key);
     }
 
+    private List<ChunkHolder> visibleChunkHoldersSnapshot() {
+        return List.copyOf(this.visibleChunkMap.values());
+    }
+
+    private List<Entry<ChunkHolder>> visibleChunkEntriesSnapshot() {
+        return this.visibleChunkMap
+            .long2ObjectEntrySet()
+            .stream()
+            .<Entry<ChunkHolder>>map(entry -> new it.unimi.dsi.fastutil.longs.AbstractLong2ObjectMap.BasicEntry<>(entry.getLongKey(), entry.getValue()))
+            .toList();
+    }
+
+    private List<ChunkHolder> updatingChunkHoldersSnapshot() {
+        synchronized (this.updatingChunkMap) {
+            return List.copyOf(this.updatingChunkMap.values());
+        }
+    }
+
+    private List<ChunkGenerationTask> drainPendingGenerationTasks() {
+        synchronized (this.pendingGenerationTasks) {
+            List<ChunkGenerationTask> tasks = List.copyOf(this.pendingGenerationTasks);
+            this.pendingGenerationTasks.clear();
+            return tasks;
+        }
+    }
+
+    private boolean hasPendingGenerationTasks() {
+        synchronized (this.pendingGenerationTasks) {
+            return !this.pendingGenerationTasks.isEmpty();
+        }
+    }
+
+    private synchronized boolean isChunkPendingDrop(long node) {
+        return this.toDrop.contains(node);
+    }
+
+    private void clearNextChunkSaveTimes() {
+        synchronized (this.nextChunkSaveTime) {
+            this.nextChunkSaveTime.clear();
+        }
+    }
+
+    private long getNextChunkSaveTime(long chunkPos) {
+        synchronized (this.nextChunkSaveTime) {
+            return this.nextChunkSaveTime.getOrDefault(chunkPos, -1L);
+        }
+    }
+
+    private void setNextChunkSaveTime(long chunkPos, long nextSaveTime) {
+        synchronized (this.nextChunkSaveTime) {
+            this.nextChunkSaveTime.put(chunkPos, nextSaveTime);
+        }
+    }
+
+    private void removeNextChunkSaveTime(long chunkPos) {
+        synchronized (this.nextChunkSaveTime) {
+            this.nextChunkSaveTime.remove(chunkPos);
+        }
+    }
+
+    private byte getChunkTypeCache(long chunkPos) {
+        synchronized (this.chunkTypeCache) {
+            return this.chunkTypeCache.get(chunkPos);
+        }
+    }
+
+    private void removeChunkTypeCache(long chunkPos) {
+        synchronized (this.chunkTypeCache) {
+            this.chunkTypeCache.remove(chunkPos);
+        }
+    }
+
     public @Nullable ChunkStatus getLatestStatus(long key) {
         ChunkHolder chunkHolder = this.getVisibleChunkIfPresent(key);
         return chunkHolder != null ? chunkHolder.getLatestStatus() : null;
@@ -346,9 +_,9 @@
             }
         });
         sb.append("Updating:").append(System.lineSeparator());
-        this.updatingChunkMap.values().forEach(addToDebug);
+        this.updatingChunkHoldersSnapshot().forEach(addToDebug);
         sb.append("Visible:").append(System.lineSeparator());
-        this.visibleChunkMap.values().forEach(addToDebug);
+        this.visibleChunkHoldersSnapshot().forEach(addToDebug);
         CrashReport report = CrashReport.forThrowable(exception, "Chunk loading");
         CrashReportCategory category = report.addCategory("Chunk loading");
         category.setDetail("Details", details);
@@ -361,7 +_,7 @@
             .thenApply(chunkResult -> chunkResult.map(list -> (LevelChunk)list.get(list.size() / 2)));
     }
 
-    private @Nullable ChunkHolder updateChunkScheduling(long node, int level, @Nullable ChunkHolder chunk, int oldLevel) {
+    private synchronized @Nullable ChunkHolder updateChunkScheduling(long node, int level, @Nullable ChunkHolder chunk, int oldLevel) {
         if (!ChunkLevel.isLoaded(oldLevel) && !ChunkLevel.isLoaded(level)) {
             return chunk;
         } else {
@@ -389,6 +_,7 @@
                 this.modified = true;
             }
 
+            net.neoforged.neoforge.event.EventHooks.fireChunkTicketLevelUpdated(this.level, node, oldLevel, level, chunk);
             return chunk;
         }
     }
@@ -411,8 +_,7 @@
 
     protected void saveAllChunks(boolean flushStorage) {
         if (flushStorage) {
-            List<ChunkHolder> chunksToSave = this.visibleChunkMap
-                .values()
+            List<ChunkHolder> chunksToSave = this.visibleChunkHoldersSnapshot()
                 .stream()
                 .filter(ChunkHolder::wasAccessibleSinceLastSave)
                 .peek(ChunkHolder::refreshAccessibility)
@@ -435,10 +_,10 @@
             this.processUnloads(() -> true);
             this.synchronize(true).join();
         } else {
-            this.nextChunkSaveTime.clear();
+            this.clearNextChunkSaveTimes();
             long now = Util.getMillis();
 
-            for (ChunkHolder chunk : this.visibleChunkMap.values()) {
+            for (ChunkHolder chunk : this.visibleChunkHoldersSnapshot()) {
                 this.saveChunkIfNeeded(chunk, now);
             }
         }
@@ -457,27 +_,24 @@
     }
 
     public boolean hasWork() {
+        boolean hasChunksToUnload;
+        synchronized (this) {
+            hasChunksToUnload = !this.pendingUnloads.isEmpty() || !this.updatingChunkMap.isEmpty() || !this.toDrop.isEmpty();
+        }
+
         return this.lightEngine.hasLightWork()
-            || !this.pendingUnloads.isEmpty()
-            || !this.updatingChunkMap.isEmpty()
+            || hasChunksToUnload
             || this.poiManager.hasWork()
-            || !this.toDrop.isEmpty()
             || !this.unloadQueue.isEmpty()
             || this.worldgenTaskDispatcher.hasWork()
             || this.lightTaskDispatcher.hasWork()
-            || this.distanceManager.hasTickets();
+            || this.distanceManager.hasTickets()
+            || this.hasPendingGenerationTasks();
     }
 
     private void processUnloads(BooleanSupplier haveTime) {
-        for (LongIterator iterator = this.toDrop.iterator(); iterator.hasNext(); iterator.remove()) {
-            long pos = iterator.nextLong();
-            ChunkHolder chunkHolder = this.updatingChunkMap.get(pos);
-            if (chunkHolder != null) {
-                this.updatingChunkMap.remove(pos);
-                this.pendingUnloads.put(pos, chunkHolder);
-                this.modified = true;
-                this.scheduleUnload(pos, chunkHolder);
-            }
+        for (Map.Entry<Long, ChunkHolder> unload : this.drainChunksToUnload()) {
+            this.scheduleUnload(unload.getKey(), unload.getValue());
         }
 
         int minimalNumberOfChunksToProcess = Math.max(0, this.unloadQueue.size() - 2000);
@@ -491,20 +_,58 @@
         this.saveChunksEagerly(haveTime);
     }
 
+    private synchronized List<Map.Entry<Long, ChunkHolder>> drainChunksToUnload() {
+        List<Map.Entry<Long, ChunkHolder>> chunksToUnload = new ArrayList<>();
+
+        for (LongIterator iterator = this.toDrop.iterator(); iterator.hasNext(); iterator.remove()) {
+            long pos = iterator.nextLong();
+            ChunkHolder chunkHolder = this.updatingChunkMap.get(pos);
+            if (chunkHolder != null) {
+                this.updatingChunkMap.remove(pos);
+                this.pendingUnloads.put(pos, chunkHolder);
+                this.modified = true;
+                chunksToUnload.add(new AbstractMap.SimpleImmutableEntry<>(pos, chunkHolder));
+            }
+        }
+
+        return chunksToUnload;
+    }
+
     private void saveChunksEagerly(BooleanSupplier haveTime) {
         long now = Util.getMillis();
         int eagerlySavedCount = 0;
-        LongIterator iterator = this.chunksToEagerlySave.iterator();
-
-        while (eagerlySavedCount < 20 && this.activeChunkWrites.get() < 128 && haveTime.getAsBoolean() && iterator.hasNext()) {
-            long chunkPos = iterator.nextLong();
+        LongSet inspectedThisPass = new LongOpenHashSet();
+
+        while (eagerlySavedCount < 20 && this.activeChunkWrites.get() < 128 && haveTime.getAsBoolean()) {
+            long chunkPos = ChunkPos.INVALID_CHUNK_POS;
+            synchronized (this.chunksToEagerlySave) {
+                LongIterator iterator = this.chunksToEagerlySave.iterator();
+                while (iterator.hasNext()) {
+                    long candidate = iterator.nextLong();
+                    if (!inspectedThisPass.contains(candidate)) {
+                        chunkPos = candidate;
+                        inspectedThisPass.add(candidate);
+                        break;
+                    }
+                }
+            }
+
+            if (chunkPos == ChunkPos.INVALID_CHUNK_POS) {
+                break;
+            }
+
             ChunkHolder chunkHolder = this.visibleChunkMap.get(chunkPos);
             ChunkAccess latestChunk = chunkHolder != null ? chunkHolder.getLatestChunk() : null;
             if (latestChunk == null || !latestChunk.isUnsaved()) {
-                iterator.remove();
+                synchronized (this.chunksToEagerlySave) {
+                    this.chunksToEagerlySave.remove(chunkPos);
+                }
             } else if (this.saveChunkIfNeeded(chunkHolder, now)) {
+                synchronized (this.chunksToEagerlySave) {
+                    this.chunksToEagerlySave.remove(chunkPos);
+                }
+
                 eagerlySavedCount++;
-                iterator.remove();
             }
         }
     }
@@ -517,19 +_,23 @@
                 this.scheduleUnload(pos, chunkHolder);
             } else {
                 ChunkAccess chunk = chunkHolder.getLatestChunk();
-                if (this.pendingUnloads.remove(pos, chunkHolder) && chunk != null) {
+                if (this.removePendingUnload(pos, chunkHolder) && chunk != null) {
                     if (chunk instanceof LevelChunk levelChunk) {
                         levelChunk.setLoaded(false);
                     }
 
                     this.save(chunk);
                     if (chunk instanceof LevelChunk levelChunk) {
+                        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.event.level.ChunkEvent.Unload(levelChunk));
                         this.level.unload(levelChunk);
                     }
 
+                    net.neoforged.neoforge.common.CommonHooks.onChunkUnload(this.poiManager, chunk); // Neo: Must be called for all chunk unloading. Not just LevelChunks.
+                    this.removeChunkTypeCache(chunk.getPos().pack()); // Neo: Prevent chunk type cache from permanently retaining data for unloaded chunks
+
                     this.lightEngine.updateChunkStatus(chunk.getPos());
                     this.lightEngine.tryScheduleUpdate();
-                    this.nextChunkSaveTime.remove(chunk.getPos().pack());
+                    this.removeNextChunkSaveTime(chunk.getPos().pack());
                 }
             }
         }, this.unloadQueue::add).whenComplete((ignored, throwable) -> {
@@ -539,7 +_,11 @@
         });
     }
 
-    protected boolean promoteChunkMap() {
+    private synchronized boolean removePendingUnload(long pos, ChunkHolder chunkHolder) {
+        return this.pendingUnloads.remove(pos, chunkHolder);
+    }
+
+    protected synchronized boolean promoteChunkMap() {
         if (!this.modified) {
             return false;
         } else {
@@ -566,6 +_,7 @@
                 Profiler.get().incrementCounter("chunkLoad");
                 if (chunkData.isPresent()) {
                     ChunkAccess chunk = chunkData.get().read(this.level, this.poiManager, this.storageInfo(), pos);
+                    net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.event.level.ChunkDataEvent.Load(chunk, chunkData.get()));
                     this.markPosition(pos, chunk.getPersistedStatus().getChunkType());
                     return chunk;
                 } else {
@@ -601,16 +_,18 @@
     }
 
     private void markPositionReplaceable(ChunkPos pos) {
-        this.chunkTypeCache.put(pos.pack(), (byte)-1);
+        this.markPosition(pos, ChunkType.PROTOCHUNK);
     }
 
     private byte markPosition(ChunkPos pos, ChunkType type) {
-        return this.chunkTypeCache.put(pos.pack(), (byte)(type == ChunkType.PROTOCHUNK ? -1 : 1));
+        synchronized (this.chunkTypeCache) {
+            return this.chunkTypeCache.put(pos.pack(), (byte)(type == ChunkType.PROTOCHUNK ? -1 : 1));
+        }
     }
 
     @Override
     public GenerationChunkHolder acquireGeneration(long chunkNode) {
-        ChunkHolder chunkHolder = this.updatingChunkMap.get(chunkNode);
+        ChunkHolder chunkHolder = this.getUpdatingChunkIfPresent(chunkNode);
         chunkHolder.increaseGenerationRefCount();
         return chunkHolder;
     }
@@ -650,7 +_,9 @@
     @Override
     public ChunkGenerationTask scheduleGenerationTask(ChunkStatus targetStatus, ChunkPos pos) {
         ChunkGenerationTask task = ChunkGenerationTask.create(this, targetStatus, pos);
-        this.pendingGenerationTasks.add(task);
+        synchronized (this.pendingGenerationTasks) {
+            this.pendingGenerationTasks.add(task);
+        }
         return task;
     }
 
@@ -666,8 +_,7 @@
 
     @Override
     public void runGenerationTasks() {
-        this.pendingGenerationTasks.forEach(this::runGenerationTask);
-        this.pendingGenerationTasks.clear();
+        this.drainPendingGenerationTasks().forEach(this::runGenerationTask);
     }
 
     public CompletableFuture<ChunkResult<LevelChunk>> prepareTickingChunk(ChunkHolder chunk) {
@@ -690,7 +_,7 @@
     private void onChunkReadyToSend(ChunkHolder chunkHolder, LevelChunk chunk) {
         ChunkPos chunkPos = chunk.getPos();
 
-        for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+        for (ServerPlayer player : this.playersForCurrentRegion()) {
             if (player.getChunkTrackingView().contains(chunkPos)) {
                 markChunkPendingToSend(player, chunk);
             }
@@ -707,7 +_,7 @@
 
     Stream<ChunkHolder> allChunksWithAtLeastStatus(ChunkStatus status) {
         int level = ChunkLevel.byStatus(status);
-        return this.visibleChunkMap.values().stream().filter(chunk -> chunk.getTicketLevel() <= level);
+        return this.visibleChunkHoldersSnapshot().stream().filter(chunk -> chunk.getTicketLevel() <= level);
     }
 
     private boolean saveChunkIfNeeded(ChunkHolder chunk, long now) {
@@ -719,14 +_,14 @@
                 return false;
             } else {
                 long chunkPos = chunkAccess.getPos().pack();
-                long nextSaveTime = this.nextChunkSaveTime.getOrDefault(chunkPos, -1L);
+                long nextSaveTime = this.getNextChunkSaveTime(chunkPos);
                 if (now < nextSaveTime) {
                     return false;
                 } else {
                     boolean saved = this.save(chunkAccess);
                     chunk.refreshAccessibility();
                     if (saved) {
-                        this.nextChunkSaveTime.put(chunkPos, now + 10000L);
+                        this.setNextChunkSaveTime(chunkPos, now + 10000L);
                     }
 
                     return saved;
@@ -759,6 +_,7 @@
                 Profiler.get().incrementCounter("chunkSave");
                 this.activeChunkWrites.incrementAndGet();
                 SerializableChunkData data = SerializableChunkData.copyOf(this.level, chunk);
+                net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.event.level.ChunkDataEvent.Save(chunk, this.level, data));
                 CompletableFuture<CompoundTag> encodedData = CompletableFuture.supplyAsync(data::write, Util.backgroundExecutor());
                 this.write(pos, encodedData::join).handle((ignored, throwable) -> {
                     if (throwable != null) {
@@ -778,7 +_,7 @@
     }
 
     private boolean isExistingChunkFull(ChunkPos pos) {
-        byte cachedChunkType = this.chunkTypeCache.get(pos.pack());
+        byte cachedChunkType = this.getChunkTypeCache(pos.pack());
         if (cachedChunkType != 0) {
             return cachedChunkType == 1;
         } else {
@@ -806,7 +_,7 @@
             this.serverViewDistance = actualNewDistance;
             this.distanceManager.updatePlayerTickets(this.serverViewDistance);
 
-            for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+            for (ServerPlayer player : this.playersForCurrentRegion()) {
                 this.updateChunkTracking(player);
             }
         }
@@ -825,9 +_,11 @@
 
     private static void markChunkPendingToSend(ServerPlayer player, LevelChunk chunk) {
         player.connection.chunkSender.markChunkPendingToSend(chunk);
+        net.neoforged.neoforge.event.EventHooks.fireChunkWatch(player, chunk, player.level());
     }
 
     private static void dropChunk(ServerPlayer player, ChunkPos pos) {
+        net.neoforged.neoforge.event.EventHooks.fireChunkUnWatch(player, pos, player.level());
         player.connection.chunkSender.dropChunk(player, pos);
     }
 
@@ -840,6 +_,34 @@
         return this.visibleChunkMap.size();
     }
 
+    int countVisibleChunksInRegion(RegionKey region) {
+        if (!region.level().equals(this.level.dimension())) {
+            return 0;
+        }
+
+        int chunks = 0;
+        for (ChunkHolder chunkHolder : this.visibleChunkHoldersSnapshot()) {
+            ChunkPos pos = chunkHolder.getPos();
+            if (chunkHolder.getTickingChunk() != null
+                    && RegionKey.fromChunk(this.level.dimension(), pos, RegionizedWorldGuard.DEFAULT_REGION_CHUNK_SHIFT).equals(region)) {
+                chunks++;
+            }
+        }
+        return chunks;
+    }
+
+    Map<RegionKey, Integer> visibleChunkCountsByRegion() {
+        Map<RegionKey, Integer> countsByRegion = new HashMap<>();
+        for (ChunkHolder chunkHolder : this.visibleChunkHoldersSnapshot()) {
+            ChunkPos pos = chunkHolder.getPos();
+            if (chunkHolder.getTickingChunk() != null) {
+                RegionKey region = RegionKey.fromChunk(this.level.dimension(), pos, RegionizedWorldGuard.DEFAULT_REGION_CHUNK_SHIFT);
+                countsByRegion.merge(region, 1, Integer::sum);
+            }
+        }
+        return countsByRegion;
+    }
+
     public net.minecraft.server.level.DistanceManager getDistanceManager() {
         return this.distanceManager;
     }
@@ -864,7 +_,7 @@
             .addColumn("fluid_ticks")
             .build(output);
 
-        for (Entry<ChunkHolder> entry : this.visibleChunkMap.long2ObjectEntrySet()) {
+        for (Entry<ChunkHolder> entry : this.visibleChunkEntriesSnapshot()) {
             long posKey = entry.getLongKey();
             ChunkPos pos = ChunkPos.unpack(posKey);
             ChunkHolder holder = entry.getValue();
@@ -935,7 +_,7 @@
             ChunkHolder holder = this.visibleChunkMap.get(spawnCandidateChunks.nextLong());
             if (holder != null) {
                 LevelChunk chunk = holder.getTickingChunk();
-                if (chunk != null && this.anyPlayerCloseEnoughForSpawningInternal(holder.getPos())) {
+                if (chunk != null && (this.anyPlayerCloseEnoughForSpawningInternal(holder.getPos()) || ticketStorage.shouldForceNaturalSpawning(holder.getPos()))) {// Neo: Force natural spawning in other chunks
                     output.add(chunk);
                 }
             }
@@ -959,10 +_,23 @@
         return triState == TriState.DEFAULT ? this.anyPlayerCloseEnoughForSpawningInternal(pos) : triState.toBoolean(true);
     }
 
+    // TODO 1.21.11 unobfuscated - seems unused and requires an AT for ticketStorage
+//    void collectForceTickingChunks(List<LevelChunk> list) {
+//        for (long l : distanceManager.ticketStorage.getForceLoadedChunks()) {
+//            ChunkHolder holder = this.visibleChunkMap.get(l);
+//            if (holder != null) {
+//                var chunk = holder.getTickingChunk();
+//                if (chunk != null) {
+//                    list.add(chunk);
+//                }
+//            }
+//        }
+//    }
+
     boolean anyPlayerCloseEnoughTo(BlockPos pos, int maxDistance) {
         Vec3 target = new Vec3(pos);
 
-        for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+        for (ServerPlayer player : this.playersForCurrentRegion()) {
             if (this.playerIsCloseEnoughTo(player, target, maxDistance)) {
                 return true;
             }
@@ -972,7 +_,7 @@
     }
 
     private boolean anyPlayerCloseEnoughForSpawningInternal(ChunkPos pos) {
-        for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+        for (ServerPlayer player : this.playersForCurrentRegion()) {
             if (this.playerIsCloseEnoughForSpawning(player, pos)) {
                 return true;
             }
@@ -988,7 +_,7 @@
         } else {
             Builder<ServerPlayer> builder = ImmutableList.builder();
 
-            for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+            for (ServerPlayer player : this.playersForCurrentRegion()) {
                 if (this.playerIsCloseEnoughForSpawning(player, pos)) {
                     builder.add(player);
                 }
@@ -1028,6 +_,35 @@
         return player.isSpectator() && !this.level.getGameRules().get(GameRules.SPECTATORS_GENERATE_CHUNKS);
     }
 
+    private List<ChunkMap.TrackedEntity> trackedEntitiesSnapshot() {
+        synchronized (this.entityMap) {
+            return new ArrayList<>(this.entityMap.values());
+        }
+    }
+
+    private @Nullable ChunkMap.TrackedEntity getTrackedEntity(Entity entity) {
+        synchronized (this.entityMap) {
+            return this.entityMap.get(entity.getId());
+        }
+    }
+
+    private boolean addTrackedEntity(Entity entity, ChunkMap.TrackedEntity trackedEntity) {
+        synchronized (this.entityMap) {
+            if (this.entityMap.containsKey(entity.getId())) {
+                return false;
+            }
+
+            this.entityMap.put(entity.getId(), trackedEntity);
+            return true;
+        }
+    }
+
+    private @Nullable ChunkMap.TrackedEntity removeTrackedEntity(Entity entity) {
+        synchronized (this.entityMap) {
+            return this.entityMap.remove(entity.getId());
+        }
+    }
+
     void updatePlayerStatus(ServerPlayer player, boolean added) {
         boolean ignored = this.skipPlayer(player);
         boolean wasIgnored = this.playerMap.ignoredOrUnknown(player);
@@ -1057,9 +_,9 @@
     }
 
     public void move(ServerPlayer player) {
-        for (ChunkMap.TrackedEntity trackedEntity : this.entityMap.values()) {
+        for (ChunkMap.TrackedEntity trackedEntity : this.trackedEntitiesSnapshot()) {
             if (trackedEntity.entity == player) {
-                trackedEntity.updatePlayers(this.level.players());
+                trackedEntity.updatePlayers(List.copyOf(this.playersForCurrentRegion()));
             } else {
                 trackedEntity.updatePlayer(player);
             }
@@ -1088,6 +_,7 @@
                 this.playerMap.unIgnorePlayer(player);
             }
 
+            //PATCH 1.20.2: Figure out the firing of the watch and unwatch events when chunk tracking updates.
             this.updateChunkTracking(player);
         }
     }
@@ -1109,7 +_,7 @@
             ChunkTrackingView previous = player.getChunkTrackingView();
             if (next instanceof ChunkTrackingView.Positioned to
                 && !(previous instanceof ChunkTrackingView.Positioned from && from.center().equals(to.center()))) {
-                player.connection.send(new ClientboundSetChunkCacheCenterPacket(to.center().x(), to.center().z()));
+                RegionizedWorldGuard.sendToPlayer(player, new ClientboundSetChunkCacheCenterPacket(to.center().x(), to.center().z()));
             }
 
             ChunkTrackingView.difference(previous, next, pos -> this.markChunkPendingToSend(player, pos), pos -> dropChunk(player, pos));
@@ -1119,7 +_,7 @@
 
     @Override
     public List<ServerPlayer> getPlayers(ChunkPos pos, boolean borderOnly) {
-        Set<ServerPlayer> allPlayers = this.playerMap.getAllPlayers();
+        Set<ServerPlayer> allPlayers = this.playersForCurrentRegion();
         Builder<ServerPlayer> result = ImmutableList.builder();
 
         for (ServerPlayer player : allPlayers) {
@@ -1132,21 +_,20 @@
     }
 
     protected void addEntity(Entity entity) {
-        if (!(entity instanceof EnderDragonPart)) {
+        if (!(entity instanceof net.neoforged.neoforge.entity.PartEntity)) {
             EntityType<?> type = entity.getType();
             int range = type.clientTrackingRange() * 16;
             if (range != 0) {
                 int updateInterval = type.updateInterval();
-                if (this.entityMap.containsKey(entity.getId())) {
+                ChunkMap.TrackedEntity trackedEntity = new ChunkMap.TrackedEntity(entity, range, updateInterval, type.trackDeltas());
+                if (!this.addTrackedEntity(entity, trackedEntity)) {
                     throw (IllegalStateException)Util.pauseInIde(new IllegalStateException("Entity is already tracked!"));
                 } else {
-                    ChunkMap.TrackedEntity trackedEntity = new ChunkMap.TrackedEntity(entity, range, updateInterval, type.trackDeltas());
-                    this.entityMap.put(entity.getId(), trackedEntity);
-                    trackedEntity.updatePlayers(this.level.players());
+                    trackedEntity.updatePlayers(List.copyOf(this.playersForCurrentRegion()));
                     if (entity instanceof ServerPlayer player) {
                         this.updatePlayerStatus(player, true);
 
-                        for (ChunkMap.TrackedEntity e : this.entityMap.values()) {
+                        for (ChunkMap.TrackedEntity e : this.trackedEntitiesSnapshot()) {
                             if (e.entity != player) {
                                 e.updatePlayer(player);
                             }
@@ -1161,26 +_,26 @@
         if (entity instanceof ServerPlayer player) {
             this.updatePlayerStatus(player, false);
 
-            for (ChunkMap.TrackedEntity trackedEntity : this.entityMap.values()) {
+            for (ChunkMap.TrackedEntity trackedEntity : this.trackedEntitiesSnapshot()) {
                 trackedEntity.removePlayer(player);
             }
         }
 
-        ChunkMap.TrackedEntity trackedEntity = this.entityMap.remove(entity.getId());
+        ChunkMap.TrackedEntity trackedEntity = this.removeTrackedEntity(entity);
         if (trackedEntity != null) {
             trackedEntity.broadcastRemoved();
         }
     }
 
     protected void tick() {
-        for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+        for (ServerPlayer player : this.playersForCurrentRegion()) {
             this.updateChunkTracking(player);
         }
 
         List<ServerPlayer> movedPlayers = Lists.newArrayList();
-        List<ServerPlayer> players = this.level.players();
+        List<ServerPlayer> players = List.copyOf(this.playersForCurrentRegion());
 
-        for (ChunkMap.TrackedEntity trackedEntity : this.entityMap.values()) {
+        for (ChunkMap.TrackedEntity trackedEntity : this.trackedEntitiesSnapshot()) {
             SectionPos oldPos = trackedEntity.lastSectionPos;
             SectionPos newPos = SectionPos.of(trackedEntity.entity);
             boolean sectionPosChanged = !Objects.equals(oldPos, newPos);
@@ -1195,46 +_,79 @@
             }
 
             if (sectionPosChanged || trackedEntity.entity.needsSync || this.distanceManager.inEntityTickingRange(newPos.chunk().pack())) {
-                trackedEntity.serverEntity.sendChanges();
+                trackedEntity.sendChanges();
             }
         }
 
         if (!movedPlayers.isEmpty()) {
-            for (ChunkMap.TrackedEntity trackedEntity : this.entityMap.values()) {
+            for (ChunkMap.TrackedEntity trackedEntity : this.trackedEntitiesSnapshot()) {
                 trackedEntity.updatePlayers(movedPlayers);
             }
         }
     }
 
+    private Set<ServerPlayer> playersForCurrentRegion() {
+        Set<ServerPlayer> players = this.playerMap.getAllPlayers();
+        players.removeIf(player -> RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level, player) == null);
+        return players;
+    }
+
     public void sendToTrackingPlayers(Entity entity, Packet<? super ClientGamePacketListener> packet) {
-        ChunkMap.TrackedEntity trackedEntity = this.entityMap.get(entity.getId());
+        if (RegionizedWorldGuard.scheduleEntityIfRegionizedOutsideRegion(
+            this.level,
+            entity,
+            "Send packet to tracking players",
+            () -> this.sendToTrackingPlayers(entity, packet)
+        )) {
+            return;
+        }
+
+        ChunkMap.TrackedEntity trackedEntity = this.getTrackedEntity(entity);
         if (trackedEntity != null) {
             trackedEntity.sendToTrackingPlayers(packet);
         }
     }
 
     public void sendToTrackingPlayersFiltered(Entity entity, Packet<? super ClientGamePacketListener> packet, Predicate<ServerPlayer> targetPredicate) {
-        ChunkMap.TrackedEntity trackedEntity = this.entityMap.get(entity.getId());
+        if (RegionizedWorldGuard.scheduleEntityIfRegionizedOutsideRegion(
+            this.level,
+            entity,
+            "Send filtered packet to tracking players",
+            () -> this.sendToTrackingPlayersFiltered(entity, packet, targetPredicate)
+        )) {
+            return;
+        }
+
+        ChunkMap.TrackedEntity trackedEntity = this.getTrackedEntity(entity);
         if (trackedEntity != null) {
             trackedEntity.sendToTrackingPlayersFiltered(packet, targetPredicate);
         }
     }
 
     protected void sendToTrackingPlayersAndSelf(Entity entity, Packet<? super ClientGamePacketListener> packet) {
-        ChunkMap.TrackedEntity trackedEntity = this.entityMap.get(entity.getId());
+        if (RegionizedWorldGuard.scheduleEntityIfRegionizedOutsideRegion(
+            this.level,
+            entity,
+            "Send packet to tracking players and self",
+            () -> this.sendToTrackingPlayersAndSelf(entity, packet)
+        )) {
+            return;
+        }
+
+        ChunkMap.TrackedEntity trackedEntity = this.getTrackedEntity(entity);
         if (trackedEntity != null) {
             trackedEntity.sendToTrackingPlayersAndSelf(packet);
         }
     }
 
     public boolean isTrackedByAnyPlayer(Entity entity) {
-        ChunkMap.TrackedEntity trackedEntity = this.entityMap.get(entity.getId());
-        return trackedEntity != null ? !trackedEntity.seenBy.isEmpty() : false;
+        ChunkMap.TrackedEntity trackedEntity = this.getTrackedEntity(entity);
+        return trackedEntity != null ? !trackedEntity.isSeenByEmpty() : false;
     }
 
     public void forEachEntityTrackedBy(ServerPlayer player, Consumer<Entity> consumer) {
-        for (ChunkMap.TrackedEntity entity : this.entityMap.values()) {
-            if (entity.seenBy.contains(player.connection)) {
+        for (ChunkMap.TrackedEntity entity : this.trackedEntitiesSnapshot()) {
+            if (entity.hasSeenBy(player.connection)) {
                 consumer.accept(entity.entity);
             }
         }
@@ -1249,7 +_,13 @@
             if (chunkAccess instanceof LevelChunk levelChunk) {
                 chunk = levelChunk;
             } else {
-                chunk = this.level.getChunk(pos.x(), pos.z());
+                ChunkAccess loadedChunk = this.level
+                    .getChunk(pos.x(), pos.z(), ChunkStatus.FULL, RegionizedWorldGuard.shouldLoadChunk(this.level, pos.x(), pos.z()));
+                if (!(loadedChunk instanceof LevelChunk levelChunk)) {
+                    continue;
+                }
+
+                chunk = levelChunk;
             }
 
             for (ServerPlayer player : this.getPlayers(pos, false)) {
@@ -1257,7 +_,9 @@
             }
         }
 
-        chunksForPlayers.forEach((playerx, chunkList) -> playerx.connection.send(ClientboundChunksBiomesPacket.forChunks((List<LevelChunk>)chunkList)));
+        chunksForPlayers.forEach((playerx, chunkList) ->
+            RegionizedWorldGuard.sendToPlayer(playerx, ClientboundChunksBiomesPacket.forChunks((List<LevelChunk>)chunkList))
+        );
     }
 
     protected PoiManager getPoiManager() {
@@ -1283,7 +_,7 @@
     }
 
     public void forEachReadyToSendChunk(Consumer<LevelChunk> consumer) {
-        for (ChunkHolder chunkHolder : this.visibleChunkMap.values()) {
+        for (ChunkHolder chunkHolder : this.visibleChunkHoldersSnapshot()) {
             LevelChunk chunk = chunkHolder.getChunkToSend();
             if (chunk != null) {
                 consumer.accept(chunk);
@@ -1291,6 +_,16 @@
         }
     }
 
+    // Neo: Getter for players watching an entity.
+    public List<ServerPlayer> getPlayersWatching(Entity entity) {
+        var trackedEntity = this.getTrackedEntity(entity);
+        if (trackedEntity != null) {
+            return java.util.Collections.unmodifiableList(trackedEntity.seenPlayersSnapshot());
+        } else {
+            return List.of();
+        }
+    }
+
     private class DistanceManager extends net.minecraft.server.level.DistanceManager {
         protected DistanceManager(TicketStorage ticketStorage, Executor executor, Executor mainThreadExecutor) {
             Objects.requireNonNull(ChunkMap.this);
@@ -1299,7 +_,7 @@
 
         @Override
         protected boolean isChunkToRemove(long node) {
-            return ChunkMap.this.toDrop.contains(node);
+            return ChunkMap.this.isChunkPendingDrop(node);
         }
 
         @Override
@@ -1317,7 +_,7 @@
         private final ServerEntity serverEntity;
         private final Entity entity;
         private final int range;
-        private SectionPos lastSectionPos;
+        private volatile SectionPos lastSectionPos;
         private final Set<ServerPlayerConnection> seenBy;
 
         public TrackedEntity(Entity entity, int range, int updateInterval, boolean trackDelta) {
@@ -1340,10 +_,76 @@
             return this.entity.getId();
         }
 
+        private List<ServerPlayerConnection> seenBySnapshot() {
+            synchronized (this.seenBy) {
+                return new ArrayList<>(this.seenBy);
+            }
+        }
+
+        private List<ServerPlayer> seenPlayersSnapshot() {
+            synchronized (this.seenBy) {
+                List<ServerPlayer> players = new ArrayList<>(this.seenBy.size());
+                for (ServerPlayerConnection connection : this.seenBy) {
+                    players.add(connection.getPlayer());
+                }
+
+                return players;
+            }
+        }
+
+        private List<ServerPlayerConnection> clearSeenBy() {
+            synchronized (this.seenBy) {
+                List<ServerPlayerConnection> snapshot = new ArrayList<>(this.seenBy);
+                this.seenBy.clear();
+                return snapshot;
+            }
+        }
+
+        private int addSeenBy(ServerPlayerConnection connection) {
+            synchronized (this.seenBy) {
+                if (!this.seenBy.add(connection)) {
+                    return -1;
+                }
+
+                return this.seenBy.size();
+            }
+        }
+
+        private boolean removeSeenBy(ServerPlayerConnection connection) {
+            synchronized (this.seenBy) {
+                return this.seenBy.remove(connection);
+            }
+        }
+
+        private boolean hasSeenBy(ServerPlayerConnection connection) {
+            synchronized (this.seenBy) {
+                return this.seenBy.contains(connection);
+            }
+        }
+
+        private boolean isSeenByEmpty() {
+            synchronized (this.seenBy) {
+                return this.seenBy.isEmpty();
+            }
+        }
+
+        private boolean scheduleTrackerOperation(String reason, Runnable task) {
+            return RegionizedTickContext.isRegionThread()
+                && RegionizedWorldGuard.scheduleEntityIfRegionizedOutsideRegion(ChunkMap.this.level, this.entity, reason, task);
+        }
+
+        private void sendChanges() {
+            if (this.scheduleTrackerOperation("Entity tracker sync", this.serverEntity::sendChanges)) {
+                return;
+            }
+
+            this.serverEntity.sendChanges();
+        }
+
         @Override
         public void sendToTrackingPlayers(Packet<? super ClientGamePacketListener> packet) {
-            for (ServerPlayerConnection connection : this.seenBy) {
-                connection.send(packet);
+            for (ServerPlayerConnection connection : this.seenBySnapshot()) {
+                RegionizedWorldGuard.sendToPlayer(connection.getPlayer(), packet);
             }
         }
 
@@ -1351,36 +_,53 @@
         public void sendToTrackingPlayersAndSelf(Packet<? super ClientGamePacketListener> packet) {
             this.sendToTrackingPlayers(packet);
             if (this.entity instanceof ServerPlayer player) {
-                player.connection.send(packet);
+                RegionizedWorldGuard.sendToPlayer(player, packet);
             }
         }
 
         @Override
         public void sendToTrackingPlayersFiltered(Packet<? super ClientGamePacketListener> packet, Predicate<ServerPlayer> targetPredicate) {
-            for (ServerPlayerConnection connection : this.seenBy) {
+            for (ServerPlayerConnection connection : this.seenBySnapshot()) {
                 if (targetPredicate.test(connection.getPlayer())) {
-                    connection.send(packet);
+                    RegionizedWorldGuard.sendToPlayer(connection.getPlayer(), packet);
                 }
             }
         }
 
         public void broadcastRemoved() {
-            for (ServerPlayerConnection connection : this.seenBy) {
+            if (this.scheduleTrackerOperation("Entity tracker removal broadcast", this::broadcastRemoved)) {
+                return;
+            }
+
+            for (ServerPlayerConnection connection : this.clearSeenBy()) {
                 this.serverEntity.removePairing(connection.getPlayer());
             }
         }
 
         public void removePlayer(ServerPlayer player) {
-            if (this.seenBy.remove(player.connection)) {
+            if (this.scheduleTrackerOperation("Entity tracker player removal", () -> this.removePlayer(player))) {
+                return;
+            }
+
+            if (this.removeSeenBy(player.connection)) {
                 this.serverEntity.removePairing(player);
-                if (this.seenBy.isEmpty()) {
-                    ChunkMap.this.level.debugSynchronizers().dropEntity(this.entity);
+                if (this.isSeenByEmpty()) {
+                    this.dropDebugEntity();
                 }
             }
         }
 
         public void updatePlayer(ServerPlayer player) {
+            if (this.scheduleTrackerOperation("Entity tracker visibility update", () -> this.updatePlayer(player))) {
+                return;
+            }
+
             if (player != this.entity) {
+                if (RegionizedWorldGuard.restrictEntityToCurrentRegion(ChunkMap.this.level, player) == null) {
+                    this.removePlayer(player);
+                    return;
+                }
+
                 Vec3 deltaToPlayer = player.position().subtract(this.entity.position());
                 int playerViewDistance = ChunkMap.this.getPlayerViewDistance(player);
                 double visibleRange = Math.min(this.getEffectiveRange(), playerViewDistance * 16);
@@ -1390,13 +_,14 @@
                     && this.entity.broadcastToPlayer(player)
                     && ChunkMap.this.isChunkTracked(player, this.entity.chunkPosition().x(), this.entity.chunkPosition().z());
                 if (visibleToPlayer) {
-                    if (this.seenBy.add(player.connection)) {
+                    int seenBySize = this.addSeenBy(player.connection);
+                    if (seenBySize != -1) {
                         this.serverEntity.addPairing(player);
-                        if (this.seenBy.size() == 1) {
-                            ChunkMap.this.level.debugSynchronizers().registerEntity(this.entity);
+                        if (seenBySize == 1) {
+                            this.registerDebugEntity();
                         }
 
-                        ChunkMap.this.level.debugSynchronizers().startTrackingEntity(player, this.entity);
+                        this.startDebugTrackingEntity(player);
                     }
                 } else {
                     this.removePlayer(player);
@@ -1422,9 +_,53 @@
         }
 
         public void updatePlayers(List<ServerPlayer> players) {
-            for (ServerPlayer player : players) {
+            List<ServerPlayer> playersSnapshot = List.copyOf(players);
+            if (this.scheduleTrackerOperation("Entity tracker visibility update", () -> this.updatePlayers(playersSnapshot))) {
+                return;
+            }
+
+            for (ServerPlayer player : playersSnapshot) {
                 this.updatePlayer(player);
             }
         }
+
+        private void registerDebugEntity() {
+            if (RegionizedWorldGuard.scheduleGlobalIfRegionized(ChunkMap.this.level, "Register debug entity", this::registerDebugEntity)) {
+                return;
+            }
+
+            ChunkMap.this.level.debugSynchronizers().registerEntity(this.entity);
+        }
+
+        private void dropDebugEntity() {
+            if (RegionizedWorldGuard.scheduleGlobalIfRegionized(ChunkMap.this.level, "Drop debug entity", this::dropDebugEntity)) {
+                return;
+            }
+
+            ChunkMap.this.level.debugSynchronizers().dropEntity(this.entity);
+        }
+
+        private void startDebugTrackingEntity(ServerPlayer player) {
+            if (RegionizedWorldGuard.scheduleGlobalIfRegionized(ChunkMap.this.level, "Start debug entity tracking", () -> this.startDebugTrackingEntity(player))) {
+                return;
+            }
+
+            ChunkMap.this.level.debugSynchronizers().startTrackingEntity(player, this.entity);
+        }
+    }
+
+    /**
+     * Neo: PR #937
+     * This is for mainly pre-generation usage such as Neoforge's generate command.
+     * Use this to schedule chunk load tasks into ChunkTaskPriorityQueueSorter so a chunk is fully finished all of their tasks before scheduling more chunks to load.
+     * Reason for this is when scheduling a huge ton of Full Status chunk tasks to the server (to load chunks),
+     * you could cause the server to only process those loading tasks and never reach the two chunk tasks that are
+     * automatically scheduled to run after the chunk is loaded to Full. As a result of flooding the system with Full Status chunk tasks,
+     * the queue for the two kind of successor chunk tasks will grow and become a memory leak of lambdas and chunk references.
+     * Use this method to schedule tasks for loading chunks in your whenCompleteAsync method call so the tasks gets processed properly over time and not leak.
+     * See {@link net.neoforged.neoforge.server.command.generation.GenerationTask#enqueueChunks} as an example usage of this method.
+     */
+    public void scheduleOnMainThreadMailbox(Runnable runnable) {
+        mainThreadExecutor.schedule(runnable);
     }
 }
