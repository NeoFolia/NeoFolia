--- a/net/minecraft/server/level/ServerBossEvent.java
+++ b/net/minecraft/server/level/ServerBossEvent.java
@@ -4,17 +_,17 @@
 import com.google.common.collect.Lists;
 import com.google.common.collect.Sets;
 import java.util.Collection;
-import java.util.Collections;
+import java.util.List;
 import java.util.Set;
 import java.util.UUID;
 import java.util.function.Function;
 import net.minecraft.network.chat.Component;
 import net.minecraft.network.protocol.game.ClientboundBossEventPacket;
 import net.minecraft.world.BossEvent;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 
 public class ServerBossEvent extends BossEvent {
     private final Set<ServerPlayer> players = Sets.newHashSet();
-    private final Set<ServerPlayer> unmodifiablePlayers = Collections.unmodifiableSet(this.players);
     private boolean visible = true;
 
     public ServerBossEvent(UUID id, Component name, BossEvent.BossBarColor color, BossEvent.BossBarOverlay overlay) {
@@ -22,7 +_,7 @@
     }
 
     @Override
-    public void setProgress(float progress) {
+    public synchronized void setProgress(float progress) {
         if (progress != this.progress) {
             super.setProgress(progress);
             this.setDirty();
@@ -31,7 +_,7 @@
     }
 
     @Override
-    public void setColor(BossEvent.BossBarColor color) {
+    public synchronized void setColor(BossEvent.BossBarColor color) {
         if (color != this.color) {
             super.setColor(color);
             this.setDirty();
@@ -40,7 +_,7 @@
     }
 
     @Override
-    public void setOverlay(BossEvent.BossBarOverlay overlay) {
+    public synchronized void setOverlay(BossEvent.BossBarOverlay overlay) {
         if (overlay != this.overlay) {
             super.setOverlay(overlay);
             this.setDirty();
@@ -49,7 +_,7 @@
     }
 
     @Override
-    public BossEvent setDarkenScreen(boolean darkenScreen) {
+    public synchronized BossEvent setDarkenScreen(boolean darkenScreen) {
         if (darkenScreen != this.darkenScreen) {
             super.setDarkenScreen(darkenScreen);
             this.setDirty();
@@ -60,7 +_,7 @@
     }
 
     @Override
-    public BossEvent setPlayBossMusic(boolean playBossMusic) {
+    public synchronized BossEvent setPlayBossMusic(boolean playBossMusic) {
         if (playBossMusic != this.playBossMusic) {
             super.setPlayBossMusic(playBossMusic);
             this.setDirty();
@@ -71,7 +_,7 @@
     }
 
     @Override
-    public BossEvent setCreateWorldFog(boolean createWorldFog) {
+    public synchronized BossEvent setCreateWorldFog(boolean createWorldFog) {
         if (createWorldFog != this.createWorldFog) {
             super.setCreateWorldFog(createWorldFog);
             this.setDirty();
@@ -82,7 +_,7 @@
     }
 
     @Override
-    public void setName(Component name) {
+    public synchronized void setName(Component name) {
         if (!Objects.equal(name, this.name)) {
             super.setName(name);
             this.setDirty();
@@ -90,29 +_,29 @@
         }
     }
 
-    private void broadcast(Function<BossEvent, ClientboundBossEventPacket> factory) {
+    private synchronized void broadcast(Function<BossEvent, ClientboundBossEventPacket> factory) {
         if (this.visible) {
             ClientboundBossEventPacket packet = factory.apply(this);
 
-            for (ServerPlayer player : this.players) {
-                player.connection.send(packet);
+            for (ServerPlayer player : List.copyOf(this.players)) {
+                this.sendToPlayer(player, packet);
             }
         }
     }
 
-    public void addPlayer(ServerPlayer player) {
+    public synchronized void addPlayer(ServerPlayer player) {
         if (this.players.add(player) && this.visible) {
-            player.connection.send(ClientboundBossEventPacket.createAddPacket(this));
+            this.sendToPlayer(player, ClientboundBossEventPacket.createAddPacket(this));
         }
     }
 
-    public void removePlayer(ServerPlayer player) {
+    public synchronized void removePlayer(ServerPlayer player) {
         if (this.players.remove(player) && this.visible) {
-            player.connection.send(ClientboundBossEventPacket.createRemovePacket(this.getId()));
+            this.sendToPlayer(player, ClientboundBossEventPacket.createRemovePacket(this.getId()));
         }
     }
 
-    public void removeAllPlayers() {
+    public synchronized void removeAllPlayers() {
         if (!this.players.isEmpty()) {
             for (ServerPlayer player : Lists.newArrayList(this.players)) {
                 this.removePlayer(player);
@@ -120,24 +_,27 @@
         }
     }
 
-    public boolean isVisible() {
+    public synchronized boolean isVisible() {
         return this.visible;
     }
 
-    public void setVisible(boolean visible) {
+    public synchronized void setVisible(boolean visible) {
         if (visible != this.visible) {
             this.visible = visible;
             this.setDirty();
 
-            for (ServerPlayer player : this.players) {
-                player.connection
-                    .send(visible ? ClientboundBossEventPacket.createAddPacket(this) : ClientboundBossEventPacket.createRemovePacket(this.getId()));
+            for (ServerPlayer player : List.copyOf(this.players)) {
+                this.sendToPlayer(player, visible ? ClientboundBossEventPacket.createAddPacket(this) : ClientboundBossEventPacket.createRemovePacket(this.getId()));
             }
         }
     }
 
-    public Collection<ServerPlayer> getPlayers() {
-        return this.unmodifiablePlayers;
+    private void sendToPlayer(ServerPlayer player, ClientboundBossEventPacket packet) {
+        RegionizedWorldGuard.sendToPlayer(player, packet);
+    }
+
+    public synchronized Collection<ServerPlayer> getPlayers() {
+        return Set.copyOf(this.players);
     }
 
     protected void setDirty() {
