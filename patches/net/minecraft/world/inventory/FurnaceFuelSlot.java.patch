--- a/net/minecraft/world/inventory/FurnaceFuelSlot.java
+++ b/net/minecraft/world/inventory/FurnaceFuelSlot.java
@@ -13,12 +_,12 @@
     }
 
     @Override
-    public boolean mayPlace(ItemStack itemStack) {
+    public synchronized boolean mayPlace(ItemStack itemStack) {
         return this.menu.isFuel(itemStack) || isBucket(itemStack);
     }
 
     @Override
-    public int getMaxStackSize(ItemStack itemStack) {
+    public synchronized int getMaxStackSize(ItemStack itemStack) {
         return isBucket(itemStack) ? 1 : super.getMaxStackSize(itemStack);
     }
 
