--- a/net/minecraft/world/inventory/Slot.java
+++ b/net/minecraft/world/inventory/Slot.java
@@ -21,80 +_,111 @@
         this.y = y;
     }
 
-    public void onQuickCraft(ItemStack picked, ItemStack original) {
+    public synchronized void onQuickCraft(ItemStack picked, ItemStack original) {
         int count = original.getCount() - picked.getCount();
         if (count > 0) {
             this.onQuickCraft(original, count);
         }
     }
 
-    protected void onQuickCraft(ItemStack picked, int count) {
-    }
-
-    protected void onSwapCraft(int count) {
-    }
-
-    protected void checkTakeAchievements(ItemStack carried) {
-    }
-
-    public void onTake(Player player, ItemStack carried) {
+    protected synchronized void onQuickCraft(ItemStack picked, int count) {
+    }
+
+    protected synchronized void onSwapCraft(int count) {
+    }
+
+    protected synchronized void checkTakeAchievements(ItemStack carried) {
+    }
+
+    public synchronized void onTake(Player player, ItemStack carried) {
         this.setChanged();
     }
 
-    public boolean mayPlace(ItemStack itemStack) {
+    public synchronized boolean mayPlace(ItemStack itemStack) {
         return true;
     }
 
-    public ItemStack getItem() {
+    public synchronized ItemStack getItem() {
         return this.container.getItem(this.slot);
     }
 
-    public boolean hasItem() {
+    public synchronized boolean hasItem() {
         return !this.getItem().isEmpty();
     }
 
-    public void setByPlayer(ItemStack itemStack) {
+    public synchronized void setByPlayer(ItemStack itemStack) {
         this.setByPlayer(itemStack, this.getItem());
     }
 
-    public void setByPlayer(ItemStack itemStack, ItemStack previous) {
+    public synchronized void setByPlayer(ItemStack itemStack, ItemStack previous) {
         this.set(itemStack);
     }
 
-    public void set(ItemStack itemStack) {
+    public synchronized void set(ItemStack itemStack) {
         this.container.setItem(this.slot, itemStack);
         this.setChanged();
     }
 
-    public void setChanged() {
+    public synchronized void setChanged() {
         this.container.setChanged();
     }
 
-    public int getMaxStackSize() {
+    public synchronized int getMaxStackSize() {
         return this.container.getMaxStackSize();
     }
 
-    public int getMaxStackSize(ItemStack itemStack) {
+    public synchronized int getMaxStackSize(ItemStack itemStack) {
         return Math.min(this.getMaxStackSize(), itemStack.getMaxStackSize());
     }
 
-    public @Nullable Identifier getNoItemIcon() {
-        return null;
+    public synchronized @Nullable Identifier getNoItemIcon() {
+        return backgroundSprite;
     }
 
-    public ItemStack remove(int amount) {
+    public synchronized ItemStack remove(int amount) {
         return this.container.removeItem(this.slot, amount);
     }
 
-    public boolean mayPickup(Player player) {
-        return true;
-    }
-
-    public boolean isActive() {
-        return true;
-    }
-
-    public Optional<ItemStack> tryRemove(int amount, int maxAmount, Player player) {
+    public synchronized boolean mayPickup(Player player) {
+        return true;
+    }
+
+    public synchronized boolean isActive() {
+        return true;
+    }
+
+    /**
+     * Retrieves the index in the inventory for this slot, this value should typically not
+     * be used, but can be useful for some occasions.
+     *
+     * @return Index in associated inventory for this slot.
+     */
+    public synchronized int getSlotIndex() {
+        return slot;
+    }
+
+    /**
+     * Checks if the other slot is in the same inventory, by comparing the inventory reference.
+     * @param other
+     * @return true if the other slot is in the same inventory
+     */
+    public synchronized boolean isSameInventory(Slot other) {
+        return this.container == other.container;
+    }
+
+    private Identifier backgroundSprite;
+    /**
+     * Sets the background atlas and sprite location.
+     *
+     * @param sprite The sprite located on that atlas.
+     * @return this, to allow chaining.
+     */
+    public synchronized Slot setBackground(Identifier sprite) {
+         this.backgroundSprite = sprite;
+         return this;
+    }
+
+    public synchronized Optional<ItemStack> tryRemove(int amount, int maxAmount, Player player) {
         if (!this.mayPickup(player)) {
             return Optional.empty();
         } else if (!this.allowModification(player) && maxAmount < this.getItem().getCount()) {
@@ -114,17 +_,17 @@
         }
     }
 
-    public ItemStack safeTake(int amount, int maxAmount, Player player) {
+    public synchronized ItemStack safeTake(int amount, int maxAmount, Player player) {
         Optional<ItemStack> result = this.tryRemove(amount, maxAmount, player);
         result.ifPresent(item -> this.onTake(player, item));
         return result.orElse(ItemStack.EMPTY);
     }
 
-    public ItemStack safeInsert(ItemStack stack) {
+    public synchronized ItemStack safeInsert(ItemStack stack) {
         return this.safeInsert(stack, stack.getCount());
     }
 
-    public ItemStack safeInsert(ItemStack inputStack, int inputAmount) {
+    public synchronized ItemStack safeInsert(ItemStack inputStack, int inputAmount) {
         if (!inputStack.isEmpty() && this.mayPlace(inputStack)) {
             ItemStack slotStack = this.getItem();
             int transferableItemCount = Math.min(Math.min(inputAmount, inputStack.getCount()), this.getMaxStackSize(inputStack) - slotStack.getCount());
@@ -146,19 +_,19 @@
         }
     }
 
-    public boolean allowModification(Player player) {
+    public synchronized boolean allowModification(Player player) {
         return this.mayPickup(player) && this.mayPlace(this.getItem());
     }
 
-    public int getContainerSlot() {
+    public synchronized int getContainerSlot() {
         return this.slot;
     }
 
-    public boolean isHighlightable() {
+    public synchronized boolean isHighlightable() {
         return true;
     }
 
-    public boolean isFake() {
+    public synchronized boolean isFake() {
         return false;
     }
 }
