--- a/net/minecraft/world/entity/animal/feline/Cat.java
+++ b/net/minecraft/world/entity/animal/feline/Cat.java
@@ -24,6 +_,7 @@
 import net.minecraft.world.attribute.EnvironmentAttributes;
 import net.minecraft.world.damagesource.DamageSource;
 import net.minecraft.world.entity.AgeableMob;
+import net.minecraft.world.entity.Entity;
 import net.minecraft.world.entity.EntitySelector;
 import net.minecraft.world.entity.EntitySpawnReason;
 import net.minecraft.world.entity.EntityType;
@@ -64,6 +_,7 @@
 import net.minecraft.world.level.storage.ValueOutput;
 import net.minecraft.world.level.storage.loot.BuiltInLootTables;
 import net.minecraft.world.phys.AABB;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class Cat extends TamableAnimal {
@@ -307,11 +_,14 @@
         this.isLyingOnTopOfSleepingPlayer = false;
         if (this.isLying()) {
             BlockPos catPos = this.blockPosition();
+            AABB searchBox = RegionizedWorldGuard.restrictToCurrentRegion(this.level(), new AABB(catPos).inflate(2.0, 2.0, 2.0));
 
-            for (Player player : this.level().getEntitiesOfClass(Player.class, new AABB(catPos).inflate(2.0, 2.0, 2.0))) {
-                if (player.isSleeping()) {
-                    this.isLyingOnTopOfSleepingPlayer = true;
-                    break;
+            if (searchBox != null) {
+                for (Player player : this.level().getEntitiesOfClass(Player.class, searchBox)) {
+                    if (player.isSleeping()) {
+                        this.isLyingOnTopOfSleepingPlayer = true;
+                        break;
+                    }
                 }
             }
         }
@@ -390,7 +_,7 @@
     ) {
         groupData = super.finalizeSpawn(level, difficulty, spawnReason, groupData);
         VariantUtils.selectVariantToSpawn(SpawnContext.create(level, this.blockPosition()), Registries.CAT_VARIANT).ifPresent(this::setVariant);
-        this.setSoundVariant(CatSoundVariants.pickRandomSoundVariant(this.registryAccess(), level.getRandom()));
+        this.setSoundVariant(CatSoundVariants.pickRandomSoundVariant(this.registryAccess(), this.getRandom()));
         return groupData;
     }
 
@@ -473,7 +_,7 @@
     }
 
     private void tryToTame(Player player) {
-        if (this.random.nextInt(3) == 0) {
+        if (this.random.nextInt(3) == 0 && !net.neoforged.neoforge.event.EventHooks.onAnimalTame(this, player)) {
             this.tame(player);
             this.setOrderedToSit(true);
             this.level().broadcastEntityEvent(this, (byte)7);
@@ -523,7 +_,7 @@
             } else if (this.cat.isOrderedToSit()) {
                 return false;
             } else {
-                LivingEntity owner = this.cat.getOwner();
+                LivingEntity owner = RegionizedWorldGuard.restrictEntityToCurrentRegion(this.cat.level(), this.cat.getOwner());
                 if (owner instanceof Player playerOwner) {
                     this.ownerPlayer = playerOwner;
                     if (!owner.isSleeping()) {
@@ -535,6 +_,10 @@
                     }
 
                     BlockPos ownerPos = this.ownerPlayer.blockPosition();
+                    if (!Cat.canReadBlock(this.cat.level(), ownerPos)) {
+                        return false;
+                    }
+
                     BlockState ownerPosState = this.cat.level().getBlockState(ownerPos);
                     if (ownerPosState.is(BlockTags.BEDS)) {
                         this.goalPos = ownerPosState.getOptionalValue(BedBlock.FACING)
@@ -549,9 +_,12 @@
         }
 
         private boolean spaceIsOccupied() {
-            for (Cat otherCat : this.cat.level().getEntitiesOfClass(Cat.class, new AABB(this.goalPos).inflate(2.0))) {
-                if (otherCat != this.cat && (otherCat.isLying() || otherCat.isRelaxStateOne())) {
-                    return true;
+            AABB searchBox = RegionizedWorldGuard.restrictToCurrentRegion(this.cat.level(), new AABB(this.goalPos).inflate(2.0));
+            if (searchBox != null) {
+                for (Cat otherCat : this.cat.level().getEntitiesOfClass(Cat.class, searchBox)) {
+                    if (otherCat != this.cat && (otherCat.isLying() || otherCat.isRelaxStateOne())) {
+                        return true;
+                    }
                 }
             }
 
@@ -560,10 +_,15 @@
 
         @Override
         public boolean canContinueToUse() {
+            Player owner = this.currentOwnerPlayer();
+            if (owner == null) {
+                return false;
+            }
+
+            this.ownerPlayer = owner;
             return this.cat.isTame()
                 && !this.cat.isOrderedToSit()
-                && this.ownerPlayer != null
-                && this.ownerPlayer.isSleeping()
+                && owner.isSleeping()
                 && this.goalPos != null
                 && !this.spaceIsOccupied();
         }
@@ -579,8 +_,10 @@
         @Override
         public void stop() {
             this.cat.setLying(false);
-            if (this.ownerPlayer.getSleepTimer() >= 100
-                && this.cat.level().getRandom().nextFloat()
+            Player owner = this.currentOwnerPlayer();
+            if (owner != null
+                && owner.getSleepTimer() >= 100
+                && this.cat.getRandom().nextFloat()
                     < this.cat.level().environmentAttributes().getValue(EnvironmentAttributes.CAT_WAKING_UP_GIFT_CHANCE, this.cat.position())) {
                 this.giveMorningGift();
             }
@@ -593,7 +_,8 @@
         private void giveMorningGift() {
             RandomSource random = this.cat.getRandom();
             BlockPos.MutableBlockPos catPos = new BlockPos.MutableBlockPos();
-            catPos.set(this.cat.isLeashed() ? this.cat.getLeashHolder().blockPosition() : this.cat.blockPosition());
+            Entity leashHolder = this.cat.getLeashHolder();
+            catPos.set(leashHolder != null ? leashHolder.blockPosition() : this.cat.blockPosition());
             this.cat
                 .randomTeleport(catPos.getX() + random.nextInt(11) - 5, catPos.getY() + random.nextInt(5) - 2, catPos.getZ() + random.nextInt(11) - 5, false);
             catPos.set(this.cat.blockPosition());
@@ -615,16 +_,18 @@
 
         @Override
         public void tick() {
-            if (this.ownerPlayer != null && this.goalPos != null) {
+            Player owner = this.currentOwnerPlayer();
+            if (owner != null && this.goalPos != null) {
+                this.ownerPlayer = owner;
                 this.cat.setInSittingPose(false);
                 this.cat.getNavigation().moveTo(this.goalPos.getX(), this.goalPos.getY(), this.goalPos.getZ(), 1.1F);
-                if (this.cat.distanceToSqr(this.ownerPlayer) < 2.5) {
+                if (this.cat.distanceToSqr(owner) < 2.5) {
                     this.onBedTicks++;
                     if (this.onBedTicks > this.adjustedTickDelay(16)) {
                         this.cat.setLying(true);
                         this.cat.setRelaxStateOne(false);
                     } else {
-                        this.cat.lookAt(this.ownerPlayer, 45.0F, 45.0F);
+                        this.cat.lookAt(owner, 45.0F, 45.0F);
                         this.cat.setRelaxStateOne(true);
                     }
                 } else {
@@ -632,6 +_,14 @@
                 }
             }
         }
+
+        private @Nullable Player currentOwnerPlayer() {
+            return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.cat.level(), this.ownerPlayer);
+        }
+    }
+
+    private static boolean canReadBlock(Level level, BlockPos pos) {
+        return RegionizedWorldGuard.shouldLoadChunk(level, pos.getX() >> 4, pos.getZ() >> 4);
     }
 
     private static class CatTemptGoal extends TemptGoal {
