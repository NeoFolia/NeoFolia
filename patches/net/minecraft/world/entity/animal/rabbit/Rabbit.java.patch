--- a/net/minecraft/world/entity/animal/rabbit/Rabbit.java
+++ b/net/minecraft/world/entity/animal/rabbit/Rabbit.java
@@ -75,6 +_,8 @@
 import net.minecraft.world.level.storage.ValueInput;
 import net.minecraft.world.level.storage.ValueOutput;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedTickContext;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class Rabbit extends Animal {
@@ -221,7 +_,7 @@
             }
 
             if (this.getVariant() == Rabbit.Variant.EVIL && this.jumpDelayTicks == 0) {
-                LivingEntity target = this.getTarget();
+                LivingEntity target = this.regionSafeTarget(this.getTarget());
                 if (target != null && this.distanceToSqr(target) < 16.0) {
                     this.facePoint(target.getX(), target.getZ());
                     this.moveControl.setWantedPosition(target.getX(), target.getY(), target.getZ(), this.moveControl.getSpeedModifier());
@@ -259,6 +_,14 @@
         this.setYRot((float)(Mth.atan2(faceZ - this.getZ(), faceX - this.getX()) * 180.0F / (float)Math.PI) - 90.0F);
     }
 
+    private @Nullable LivingEntity regionSafeTarget(@Nullable LivingEntity target) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), target);
+    }
+
+    protected static boolean canReadBlock(LevelReader level, BlockPos pos) {
+        return !(level instanceof Level levelInstance) || RegionizedWorldGuard.shouldLoadChunk(levelInstance, pos.getX() >> 4, pos.getZ() >> 4);
+    }
+
     private void enableJumpControl() {
         ((Rabbit.RabbitJumpControl)this.jumpControl).setCanJump(true);
     }
@@ -408,7 +_,7 @@
     public @Nullable SpawnGroupData finalizeSpawn(
         ServerLevelAccessor level, DifficultyInstance difficulty, EntitySpawnReason spawnReason, @Nullable SpawnGroupData groupData
     ) {
-        Rabbit.Variant variant = getRandomRabbitVariant(level, this.blockPosition());
+        Rabbit.Variant variant = getRandomRabbitVariant(level, this.blockPosition(), this.getRandom());
         if (groupData instanceof Rabbit.RabbitGroupData) {
             variant = ((Rabbit.RabbitGroupData)groupData).variant;
         } else {
@@ -420,8 +_,12 @@
     }
 
     private static Rabbit.Variant getRandomRabbitVariant(LevelAccessor level, BlockPos pos) {
+        return getRandomRabbitVariant(level, pos, RegionizedTickContext.currentRandom().orElse(level.getRandom()));
+    }
+
+    private static Rabbit.Variant getRandomRabbitVariant(LevelAccessor level, BlockPos pos, RandomSource random) {
         Holder<Biome> biome = level.getBiome(pos);
-        int randomVal = level.getRandom().nextInt(100);
+        int randomVal = random.nextInt(100);
         if (biome.is(BiomeTags.SPAWNS_WHITE_RABBITS)) {
             return randomVal < 80 ? Rabbit.Variant.WHITE : Rabbit.Variant.WHITE_SPLOTCHED;
         } else if (biome.is(BiomeTags.SPAWNS_GOLD_RABBITS)) {
@@ -432,7 +_,8 @@
     }
 
     public static boolean checkRabbitSpawnRules(EntityType<Rabbit> type, LevelAccessor level, EntitySpawnReason spawnReason, BlockPos pos, RandomSource random) {
-        return level.getBlockState(pos.below()).is(BlockTags.RABBITS_SPAWNABLE_ON) && isBrightEnoughToSpawn(level, pos);
+        BlockPos below = pos.below();
+        return canReadBlock(level, below) && level.getBlockState(below).is(BlockTags.RABBITS_SPAWNABLE_ON) && isBrightEnoughToSpawn(level, pos);
     }
 
     private boolean wantsMoreFood() {
@@ -600,7 +_,7 @@
         @Override
         public boolean canUse() {
             if (this.nextStartTick <= 0) {
-                if (!getServerLevel(this.rabbit).getGameRules().get(GameRules.MOB_GRIEFING)) {
+                if (!net.neoforged.neoforge.event.EventHooks.canEntityGrief(getServerLevel(this.rabbit.level()), this.rabbit)) {
                     return false;
                 }
 
@@ -625,6 +_,12 @@
             if (this.isReachedTarget()) {
                 Level level = this.rabbit.level();
                 BlockPos cropsPos = this.blockPos.above();
+                if (!Rabbit.canReadBlock(level, cropsPos)) {
+                    this.canRaid = false;
+                    this.nextStartTick = 10;
+                    return;
+                }
+
                 BlockState blockState = level.getBlockState(cropsPos);
                 Block block = blockState.getBlock();
                 if (this.canRaid && block instanceof CarrotBlock) {
@@ -648,8 +_,16 @@
 
         @Override
         protected boolean isValidTarget(LevelReader level, BlockPos pos) {
+            if (!Rabbit.canReadBlock(level, pos)) {
+                return false;
+            }
+
             BlockState state = level.getBlockState(pos);
             if (state.is(BlockTags.SUPPORTS_CROPS) && this.wantsToRaid && !this.canRaid) {
+                if (!Rabbit.canReadBlock(level, pos.above())) {
+                    return false;
+                }
+
                 state = level.getBlockState(pos.above());
                 if (state.getBlock() instanceof CarrotBlock carrotBlock && carrotBlock.isMaxAge(state)) {
                     this.canRaid = true;
