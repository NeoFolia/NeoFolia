--- a/net/minecraft/world/entity/monster/skeleton/Stray.java
+++ b/net/minecraft/world/entity/monster/skeleton/Stray.java
@@ -16,6 +_,7 @@
 import net.minecraft.world.level.Level;
 import net.minecraft.world.level.ServerLevelAccessor;
 import net.minecraft.world.level.block.Blocks;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class Stray extends AbstractSkeleton {
@@ -30,10 +_,18 @@
 
         do {
             checkSkyPos = checkSkyPos.above();
+            if (!canReadBlock(level, checkSkyPos)) {
+                return false;
+            }
         } while (level.getBlockState(checkSkyPos).is(Blocks.POWDER_SNOW));
 
+        BlockPos skyCheckPos = checkSkyPos.below();
         return Monster.checkMonsterSpawnRules(type, level, spawnReason, pos, random)
-            && (EntitySpawnReason.isSpawner(spawnReason) || level.canSeeSky(checkSkyPos.below()));
+            && (EntitySpawnReason.isSpawner(spawnReason) || canReadBlock(level, skyCheckPos) && level.canSeeSky(skyCheckPos));
+    }
+
+    private static boolean canReadBlock(ServerLevelAccessor level, BlockPos pos) {
+        return !(level instanceof Level levelInstance) || RegionizedWorldGuard.shouldLoadChunk(levelInstance, pos.getX() >> 4, pos.getZ() >> 4);
     }
 
     @Override
