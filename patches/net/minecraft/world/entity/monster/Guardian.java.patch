--- a/net/minecraft/world/entity/monster/Guardian.java
+++ b/net/minecraft/world/entity/monster/Guardian.java
@@ -42,6 +_,7 @@
 import net.minecraft.world.level.LevelReader;
 import net.minecraft.world.level.pathfinder.PathType;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class Guardian extends Monster {
@@ -134,7 +_,7 @@
                 }
             }
         } else {
-            return this.getTarget();
+            return this.regionSafeTarget(this.getTarget());
         }
     }
 
@@ -174,7 +_,9 @@
 
     @Override
     public float getWalkTargetValue(BlockPos pos, LevelReader level) {
-        return level.getFluidState(pos).is(FluidTags.WATER) ? 10.0F + level.getPathfindingCostFromLightLevels(pos) : super.getWalkTargetValue(pos, level);
+        return canReadBlock(level, pos) && level.getFluidState(pos).is(FluidTags.WATER)
+            ? 10.0F + level.getPathfindingCostFromLightLevels(pos)
+            : super.getWalkTargetValue(pos, level);
     }
 
     @Override
@@ -302,10 +_,17 @@
     public static boolean checkGuardianSpawnRules(
         EntityType<? extends Guardian> type, LevelAccessor level, EntitySpawnReason spawnReason, BlockPos pos, RandomSource random
     ) {
-        return (random.nextInt(20) == 0 || !level.canSeeSkyFromBelowWater(pos))
+        BlockPos below = pos.below();
+        return canReadBlock(level, pos)
+            && canReadBlock(level, below)
+            && (random.nextInt(20) == 0 || !level.canSeeSkyFromBelowWater(pos))
             && level.getDifficulty() != Difficulty.PEACEFUL
             && (EntitySpawnReason.isSpawner(spawnReason) || level.getFluidState(pos).is(FluidTags.WATER))
-            && level.getFluidState(pos.below()).is(FluidTags.WATER);
+            && level.getFluidState(below).is(FluidTags.WATER);
+    }
+
+    private static boolean canReadBlock(LevelReader level, BlockPos pos) {
+        return !(level instanceof Level levelInstance) || RegionizedWorldGuard.shouldLoadChunk(levelInstance, pos.getX() >> 4, pos.getZ() >> 4);
     }
 
     @Override
@@ -313,7 +_,7 @@
         if (!this.isMoving()
             && !source.is(DamageTypeTags.AVOIDS_GUARDIAN_THORNS)
             && !source.is(DamageTypes.THORNS)
-            && source.getDirectEntity() instanceof LivingEntity cause) {
+            && RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), source.getDirectEntity()) instanceof LivingEntity cause) {
             cause.hurtServer(level, this.damageSources().thorns(this), 2.0F);
         }
 
@@ -334,11 +_,15 @@
         this.moveRelative(0.1F, input);
         this.move(MoverType.SELF, this.getDeltaMovement());
         this.setDeltaMovement(this.getDeltaMovement().scale(0.9));
-        if (!this.isMoving() && this.getTarget() == null) {
+        if (!this.isMoving() && this.regionSafeTarget(this.getTarget()) == null) {
             this.setDeltaMovement(this.getDeltaMovement().add(0.0, -0.005, 0.0));
         }
     }
 
+    private @Nullable LivingEntity regionSafeTarget(@Nullable LivingEntity target) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), target);
+    }
+
     private static class GuardianAttackGoal extends Goal {
         private final Guardian guardian;
         private int attackTime;
@@ -352,21 +_,22 @@
 
         @Override
         public boolean canUse() {
-            LivingEntity target = this.guardian.getTarget();
+            LivingEntity target = this.guardian.regionSafeTarget(this.guardian.getTarget());
             return target != null && target.isAlive();
         }
 
         @Override
         public boolean canContinueToUse() {
+            LivingEntity target = this.guardian.regionSafeTarget(this.guardian.getTarget());
             return super.canContinueToUse()
-                && (this.elder || this.guardian.getTarget() != null && this.guardian.distanceToSqr(this.guardian.getTarget()) > 9.0);
+                && (this.elder || target != null && this.guardian.distanceToSqr(target) > 9.0);
         }
 
         @Override
         public void start() {
             this.attackTime = -10;
             this.guardian.getNavigation().stop();
-            LivingEntity target = this.guardian.getTarget();
+            LivingEntity target = this.guardian.regionSafeTarget(this.guardian.getTarget());
             if (target != null) {
                 this.guardian.getLookControl().setLookAt(target, 90.0F, 90.0F);
             }
@@ -388,7 +_,7 @@
 
         @Override
         public void tick() {
-            LivingEntity target = this.guardian.getTarget();
+            LivingEntity target = this.guardian.regionSafeTarget(this.guardian.getTarget());
             if (target != null) {
                 this.guardian.getNavigation().stop();
                 this.guardian.getLookControl().setLookAt(target, 90.0F, 90.0F);
