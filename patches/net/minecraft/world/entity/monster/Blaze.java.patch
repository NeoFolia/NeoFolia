--- a/net/minecraft/world/entity/monster/Blaze.java
+++ b/net/minecraft/world/entity/monster/Blaze.java
@@ -25,6 +_,8 @@
 import net.minecraft.world.level.Level;
 import net.minecraft.world.level.pathfinder.PathType;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
+import org.jspecify.annotations.Nullable;
 
 public class Blaze extends Monster {
     private float allowedHeightOffset = 0.5F;
@@ -123,7 +_,7 @@
             this.allowedHeightOffset = (float)this.random.triangle(0.5, 6.891);
         }
 
-        LivingEntity target = this.getTarget();
+        LivingEntity target = this.regionSafeTarget(this.getTarget());
         if (target != null && target.getEyeY() > this.getEyeY() + this.allowedHeightOffset && this.canAttack(target)) {
             Vec3 movement = this.getDeltaMovement();
             this.setDeltaMovement(this.getDeltaMovement().add(0.0, (0.3F - movement.y) * 0.3F, 0.0));
@@ -153,6 +_,10 @@
         this.entityData.set(DATA_FLAGS_ID, flags);
     }
 
+    private @Nullable LivingEntity regionSafeTarget(@Nullable LivingEntity target) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), target);
+    }
+
     private static class BlazeAttackGoal extends Goal {
         private final Blaze blaze;
         private int attackStep;
@@ -166,7 +_,7 @@
 
         @Override
         public boolean canUse() {
-            LivingEntity target = this.blaze.getTarget();
+            LivingEntity target = this.blaze.regionSafeTarget(this.blaze.getTarget());
             return target != null && target.isAlive() && this.blaze.canAttack(target);
         }
 
@@ -189,7 +_,7 @@
         @Override
         public void tick() {
             this.attackTime--;
-            LivingEntity target = this.blaze.getTarget();
+            LivingEntity target = this.blaze.regionSafeTarget(this.blaze.getTarget());
             if (target != null) {
                 boolean hasLineOfSight = this.blaze.getSensing().hasLineOfSight(target);
                 if (hasLineOfSight) {
