--- a/net/minecraft/world/entity/npc/villager/AbstractVillager.java
+++ b/net/minecraft/world/entity/npc/villager/AbstractVillager.java
@@ -48,6 +_,8 @@
 import net.minecraft.world.level.storage.loot.parameters.LootContextParamSets;
 import net.minecraft.world.level.storage.loot.parameters.LootContextParams;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedTickContext;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
@@ -101,6 +_,10 @@
 
     @Override
     public @Nullable Player getTradingPlayer() {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), this.tradingPlayer);
+    }
+
+    protected @Nullable Player getRawTradingPlayer() {
         return this.tradingPlayer;
     }
 
@@ -135,8 +_,18 @@
         offer.increaseUses();
         this.ambientSoundTime = -this.getAmbientSoundInterval();
         this.rewardTradeXp(offer);
-        if (this.tradingPlayer instanceof ServerPlayer) {
-            CriteriaTriggers.TRADE.trigger((ServerPlayer)this.tradingPlayer, this, offer.getResult());
+        Player safeTradingPlayer = this.getTradingPlayer();
+        if (safeTradingPlayer instanceof ServerPlayer serverPlayer) {
+            this.triggerTrade(serverPlayer, offer);
+        }
+        net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.event.entity.player.TradeWithVillagerEvent(safeTradingPlayer, offer, this));
+    }
+
+    private void triggerTrade(ServerPlayer player, MerchantOffer offer) {
+        ItemStack result = offer.getResult().copy();
+        Runnable trigger = () -> RegionizedWorldGuard.executeOnPlayerRegion(player, () -> CriteriaTriggers.TRADE.trigger(player, this, result));
+        if (!RegionizedWorldGuard.scheduleGlobalIfRegionized(this.level(), "Villager trade advancement", trigger)) {
+            trigger.run();
         }
     }
 
@@ -303,6 +_,12 @@
 
     @Override
     public boolean stillValid(Player player) {
+        if (this.level() instanceof ServerLevel serverLevel
+            && RegionizedTickContext.isRegionThread()
+            && !RegionizedWorldGuard.isRegionThreadFor(serverLevel, this.blockPosition())) {
+            return false;
+        }
+
         return this.getTradingPlayer() == player && this.isAlive() && player.isWithinEntityInteractionRange(this, 4.0);
     }
 }
