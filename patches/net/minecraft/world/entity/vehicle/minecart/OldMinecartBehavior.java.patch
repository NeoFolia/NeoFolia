--- a/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
+++ b/net/minecraft/world/entity/vehicle/minecart/OldMinecartBehavior.java
@@ -22,6 +_,7 @@
 import net.minecraft.world.level.block.state.properties.RailShape;
 import net.minecraft.world.phys.AABB;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class OldMinecartBehavior extends MinecartBehavior {
@@ -57,12 +_,12 @@
         if (this.level() instanceof ServerLevel level) {
             this.minecart.applyGravity();
             BlockPos var11 = this.minecart.getCurrentBlockPosOrRailBelow();
-            BlockState state = this.level().getBlockState(var11);
+            BlockState state = this.minecart.canReadBlock(var11) ? this.level().getBlockState(var11) : Blocks.AIR.defaultBlockState();
             boolean onRails = BaseRailBlock.isRail(state);
             this.minecart.setOnRails(onRails);
             if (onRails) {
                 this.moveAlongTrack(level);
-                if (state.is(Blocks.ACTIVATOR_RAIL)) {
+                if (state.getBlock() instanceof PoweredRailBlock poweredRail && poweredRail.isActivatorRail()) {
                     this.minecart.activateMinecart(level, var11.getX(), var11.getY(), var11.getZ(), state.getValue(PoweredRailBlock.POWERED));
                 }
             } else {
@@ -103,7 +_,17 @@
     @Override
     public void moveAlongTrack(ServerLevel level) {
         BlockPos pos = this.minecart.getCurrentBlockPosOrRailBelow();
+        if (!this.minecart.canReadBlock(pos)) {
+            this.minecart.comeOffTrack(level);
+            return;
+        }
+
         BlockState state = this.level().getBlockState(pos);
+        if (!BaseRailBlock.isRail(state)) {
+            this.minecart.comeOffTrack(level);
+            return;
+        }
+
         this.minecart.resetFallDistance();
         double x = this.minecart.getX();
         double y = this.minecart.getY();
@@ -112,7 +_,7 @@
         y = pos.getY();
         boolean powerTrack = false;
         boolean haltTrack = false;
-        if (state.is(Blocks.POWERED_RAIL)) {
+        if (state.getBlock() instanceof PoweredRailBlock poweredRail && !poweredRail.isActivatorRail()) {
             powerTrack = state.getValue(PoweredRailBlock.POWERED);
             haltTrack = !powerTrack;
         }
@@ -123,7 +_,7 @@
         }
 
         Vec3 movement = this.getDeltaMovement();
-        RailShape shape = state.getValue(((BaseRailBlock)state.getBlock()).getShapeProperty());
+        RailShape shape = ((BaseRailBlock)state.getBlock()).getRailDirection(state, this.level(), pos, this.minecart);;
         switch (shape) {
             case ASCENDING_EAST:
                 this.setDeltaMovement(movement.add(-slideSpeed, 0.0, 0.0));
@@ -275,13 +_,19 @@
         int xt = Mth.floor(x);
         int yt = Mth.floor(y);
         int zt = Mth.floor(z);
-        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).is(BlockTags.RAILS)) {
+        BlockPos railBelow = new BlockPos(xt, yt - 1, zt);
+        if (this.minecart.canReadBlock(railBelow) && this.level().getBlockState(railBelow).is(BlockTags.RAILS)) {
             yt--;
         }
 
-        BlockState state = this.level().getBlockState(new BlockPos(xt, yt, zt));
+        var railPos = new BlockPos(xt, yt, zt);
+        if (!this.minecart.canReadBlock(railPos)) {
+            return null;
+        }
+
+        BlockState state = this.level().getBlockState(railPos);
         if (BaseRailBlock.isRail(state)) {
-            RailShape shape = state.getValue(((BaseRailBlock)state.getBlock()).getShapeProperty());
+            RailShape shape = ((BaseRailBlock)state.getBlock()).getRailDirection(state, this.level(), railPos, this.minecart);
             y = yt;
             if (shape.isSlope()) {
                 y = yt + 1;
@@ -313,13 +_,19 @@
         int xt = Mth.floor(x);
         int yt = Mth.floor(y);
         int zt = Mth.floor(z);
-        if (this.level().getBlockState(new BlockPos(xt, yt - 1, zt)).is(BlockTags.RAILS)) {
+        BlockPos railBelow = new BlockPos(xt, yt - 1, zt);
+        if (this.minecart.canReadBlock(railBelow) && this.level().getBlockState(railBelow).is(BlockTags.RAILS)) {
             yt--;
         }
 
-        BlockState state = this.level().getBlockState(new BlockPos(xt, yt, zt));
+        BlockPos railPos = new BlockPos(xt, yt, zt);
+        if (!this.minecart.canReadBlock(railPos)) {
+            return null;
+        }
+
+        BlockState state = this.level().getBlockState(railPos);
         if (BaseRailBlock.isRail(state)) {
-            RailShape shape = state.getValue(((BaseRailBlock)state.getBlock()).getShapeProperty());
+            RailShape shape = ((BaseRailBlock)state.getBlock()).getRailDirection(state, this.level(), railPos, this.minecart);
             Pair<Vec3i, Vec3i> exits = AbstractMinecart.exits(shape);
             Vec3i exit0 = exits.getFirst();
             Vec3i exit1 = exits.getSecond();
@@ -366,6 +_,10 @@
     @Override
     public boolean pushAndPickupEntities() {
         AABB hitbox = this.minecart.getBoundingBox().inflate(0.2F, 0.0, 0.2F);
+        hitbox = RegionizedWorldGuard.restrictToCurrentRegion(this.level(), hitbox);
+        if (hitbox == null) {
+            return false;
+        }
         if (this.minecart.isRideable() && this.getDeltaMovement().horizontalDistanceSqr() >= 0.01) {
             List<Entity> entities = this.level().getEntities(this.minecart, hitbox, EntitySelector.pushableBy(this.minecart));
             if (!entities.isEmpty()) {
