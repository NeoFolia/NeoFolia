--- a/net/minecraft/world/entity/vehicle/DismountHelper.java
+++ b/net/minecraft/world/entity/vehicle/DismountHelper.java
@@ -4,17 +_,20 @@
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.Direction;
 import net.minecraft.tags.BlockTags;
+import net.minecraft.util.Mth;
 import net.minecraft.world.entity.EntityType;
 import net.minecraft.world.entity.LivingEntity;
 import net.minecraft.world.entity.Pose;
 import net.minecraft.world.level.BlockGetter;
 import net.minecraft.world.level.CollisionGetter;
+import net.minecraft.world.level.Level;
 import net.minecraft.world.level.block.TrapDoorBlock;
 import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.phys.AABB;
 import net.minecraft.world.phys.Vec3;
 import net.minecraft.world.phys.shapes.Shapes;
 import net.minecraft.world.phys.shapes.VoxelShape;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class DismountHelper {
@@ -39,6 +_,10 @@
     }
 
     public static boolean canDismountTo(CollisionGetter level, LivingEntity passenger, AABB box) {
+        if (!canReadBounds(level, box)) {
+            return false;
+        }
+
         for (VoxelShape collision : level.getBlockCollisions(passenger, box)) {
             if (!collision.isEmpty()) {
                 return false;
@@ -53,6 +_,10 @@
     }
 
     public static VoxelShape nonClimbableShape(BlockGetter level, BlockPos pos) {
+        if (!canReadBlock(level, pos)) {
+            return Shapes.block();
+        }
+
         BlockState blockState = level.getBlockState(pos);
         return !blockState.is(BlockTags.CLIMBABLE) && (!(blockState.getBlock() instanceof TrapDoorBlock) || !blockState.getValue(TrapDoorBlock.OPEN))
             ? blockState.getCollisionShape(level, pos)
@@ -77,6 +_,10 @@
     }
 
     public static @Nullable Vec3 findSafeDismountLocation(EntityType<?> type, CollisionGetter level, BlockPos blockPos, boolean checkDangerous) {
+        if (!canReadBlock(level, blockPos) || !canReadBlock(level, blockPos.below()) || !canReadBlock(level, blockPos.above())) {
+            return null;
+        }
+
         if (checkDangerous && type.isBlockDangerous(level.getBlockState(blockPos))) {
             return null;
         } else {
@@ -88,6 +_,9 @@
             } else {
                 Vec3 position = Vec3.upFromBottomCenterOf(blockPos, floorHeight);
                 AABB aabb = type.getDimensions().makeBoundingBox(position);
+                if (!canReadBounds(level, aabb)) {
+                    return null;
+                }
 
                 for (VoxelShape shape : level.getBlockCollisions(null, aabb)) {
                     if (!shape.isEmpty()) {
@@ -104,5 +_,30 @@
                 }
             }
         }
+    }
+
+    private static boolean canReadBlock(BlockGetter level, BlockPos pos) {
+        return !(level instanceof Level levelInstance)
+            || RegionizedWorldGuard.shouldLoadChunk(levelInstance, pos.getX() >> 4, pos.getZ() >> 4);
+    }
+
+    private static boolean canReadBounds(CollisionGetter level, AABB box) {
+        if (!(level instanceof Level levelInstance)) {
+            return true;
+        }
+
+        int minChunkX = Mth.floor(box.minX) >> 4;
+        int maxChunkX = Mth.floor(box.maxX) >> 4;
+        int minChunkZ = Mth.floor(box.minZ) >> 4;
+        int maxChunkZ = Mth.floor(box.maxZ) >> 4;
+        for (int chunkX = minChunkX; chunkX <= maxChunkX; chunkX++) {
+            for (int chunkZ = minChunkZ; chunkZ <= maxChunkZ; chunkZ++) {
+                if (!RegionizedWorldGuard.shouldLoadChunk(levelInstance, chunkX, chunkZ)) {
+                    return false;
+                }
+            }
+        }
+
+        return true;
     }
 }
