--- a/net/minecraft/world/entity/AreaEffectCloud.java
+++ b/net/minecraft/world/entity/AreaEffectCloud.java
@@ -23,6 +_,8 @@
 import net.minecraft.world.level.material.PushReaction;
 import net.minecraft.world.level.storage.ValueInput;
 import net.minecraft.world.level.storage.ValueOutput;
+import net.minecraft.world.phys.AABB;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class AreaEffectCloud extends Entity implements TraceableEntity {
@@ -216,7 +_,8 @@
                     } else {
                         List<MobEffectInstance> allEffects = new ArrayList<>();
                         this.potionContents.forEachEffect(allEffects::add, this.potionDurationScale);
-                        List<LivingEntity> entities = this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox());
+                        AABB regionBox = RegionizedWorldGuard.restrictToCurrentRegion(this.level(), this.getBoundingBox());
+                        List<LivingEntity> entities = regionBox == null ? List.of() : this.level().getEntitiesOfClass(LivingEntity.class, regionBox);
                         if (!entities.isEmpty()) {
                             for (LivingEntity entity : entities) {
                                 if (!this.victims.containsKey(entity) && entity.isAffectedByPotions() && !allEffects.stream().noneMatch(entity::canBeAffected)) {
@@ -300,7 +_,7 @@
     }
 
     public @Nullable LivingEntity getOwner() {
-        return EntityReference.getLivingEntity(this.owner, this.level());
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.level(), EntityReference.getLivingEntity(this.owner, this.level()));
     }
 
     @Override
