--- a/net/minecraft/world/entity/boss/enderdragon/phases/AbstractDragonSittingPhase.java
+++ b/net/minecraft/world/entity/boss/enderdragon/phases/AbstractDragonSittingPhase.java
@@ -1,9 +_,11 @@
 package net.minecraft.world.entity.boss.enderdragon.phases;
 
 import net.minecraft.world.damagesource.DamageSource;
+import net.minecraft.world.entity.Entity;
 import net.minecraft.world.entity.boss.enderdragon.EnderDragon;
 import net.minecraft.world.entity.projectile.arrow.AbstractArrow;
 import net.minecraft.world.entity.projectile.hurtingprojectile.windcharge.WindCharge;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 
 public abstract class AbstractDragonSittingPhase extends AbstractDragonPhaseInstance {
     public AbstractDragonSittingPhase(EnderDragon dragon) {
@@ -17,10 +_,11 @@
 
     @Override
     public float onHurt(DamageSource source, float damage) {
-        if (!(source.getDirectEntity() instanceof AbstractArrow) && !(source.getDirectEntity() instanceof WindCharge)) {
+        Entity directEntity = RegionizedWorldGuard.restrictEntityToCurrentRegion(this.dragon.level(), source.getDirectEntity());
+        if (!(directEntity instanceof AbstractArrow) && !(directEntity instanceof WindCharge)) {
             return super.onHurt(source, damage);
         } else {
-            source.getDirectEntity().igniteForSeconds(1.0F);
+            directEntity.igniteForSeconds(1.0F);
             return 0.0F;
         }
     }
