--- a/net/minecraft/world/entity/ai/memory/ExpirableValue.java
+++ b/net/minecraft/world/entity/ai/memory/ExpirableValue.java
@@ -15,10 +_,10 @@
 
     @Override
     public String toString() {
-        return this.value + (this.timeToLive.isPresent() ? " (ttl: " + this.timeToLive.get() + ")" : "");
+        return this.value + this.timeToLive.map(ttl -> " (ttl: " + ttl + ")").orElse("");
     }
 
-    public static <T> Codec<ExpirableValue<T>> codec(Codec<T> valueCodec) {
+    public static synchronized <T> Codec<ExpirableValue<T>> codec(Codec<T> valueCodec) {
         return RecordCodecBuilder.create(
             i -> i.group(
                     valueCodec.fieldOf("value").forGetter(ExpirableValue::value),
