--- a/net/minecraft/world/entity/ai/behavior/VillagerGoalPackages.java
+++ b/net/minecraft/world/entity/ai/behavior/VillagerGoalPackages.java
@@ -22,6 +_,7 @@
 import net.minecraft.world.entity.raid.Raid;
 import net.minecraft.world.level.block.BedBlock;
 import net.minecraft.world.level.block.state.BlockState;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 
 public class VillagerGoalPackages {
     private static final float STROLL_SPEED_MODIFIER = 0.4F;
@@ -29,7 +_,7 @@
     public static final int INTERACT_WALKUP_DIST = 2;
     public static final float INTERACT_SPEED_MODIFIER = 0.5F;
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getCorePackage(
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getCorePackage(
         Holder<VillagerProfession> profession, float speedModifier
     ) {
         return ImmutableList.of(
@@ -66,12 +_,16 @@
         );
     }
 
-    private static boolean validateBedPoi(ServerLevel level, BlockPos blockPos) {
+    private static synchronized boolean validateBedPoi(ServerLevel level, BlockPos blockPos) {
+        if (!RegionizedWorldGuard.shouldLoadChunk(level, blockPos.getX() >> 4, blockPos.getZ() >> 4)) {
+            return false;
+        }
+
         BlockState blockState = level.getBlockState(blockPos);
         return blockState.is(BlockTags.BEDS) && !blockState.getValue(BedBlock.OCCUPIED);
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getWorkPackage(
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getWorkPackage(
         Holder<VillagerProfession> profession, float speedModifier
     ) {
         WorkAtPoi workAtPoi;
@@ -104,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPlayPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPlayPackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(0, new MoveToTargetSink(80, 120)),
             getFullLookBehavior(),
@@ -127,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getRestPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getRestPackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(2, SetWalkTargetFromBlockMemory.create(MemoryModuleType.HOME, speedModifier, 1, 150, 1200)),
             Pair.of(3, ValidateNearbyPoi.create(p -> p.is(PoiTypes.HOME), MemoryModuleType.HOME)),
@@ -149,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getMeetPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getMeetPackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(
                 2,
@@ -177,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getIdlePackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getIdlePackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(
                 2,
@@ -224,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPanicPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPanicPackage(float speedModifier) {
         float runawaySpeed = speedModifier * 1.5F;
         return ImmutableList.of(
             Pair.of(0, VillagerCalmDown.create()),
@@ -235,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPreRaidPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getPreRaidPackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(0, RingBell.create()),
             Pair.of(
@@ -252,7 +_,7 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getRaidPackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getRaidPackage(float speedModifier) {
         return ImmutableList.of(
             Pair.of(
                 0,
@@ -277,14 +_,14 @@
         );
     }
 
-    public static ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getHidePackage(float speedModifier) {
+    public static synchronized ImmutableList<Pair<Integer, ? extends BehaviorControl<? super Villager>>> getHidePackage(float speedModifier) {
         int closeEnoughDist = 2;
         return ImmutableList.of(
             Pair.of(0, SetHiddenState.create(15, 3)), Pair.of(1, LocateHidingPlace.create(32, speedModifier * 1.25F, 2)), getMinimalLookBehavior()
         );
     }
 
-    private static Pair<Integer, BehaviorControl<LivingEntity>> getFullLookBehavior() {
+    private static synchronized Pair<Integer, BehaviorControl<LivingEntity>> getFullLookBehavior() {
         return Pair.of(
             5,
             new RunOne<>(
@@ -304,7 +_,7 @@
         );
     }
 
-    private static Pair<Integer, BehaviorControl<LivingEntity>> getMinimalLookBehavior() {
+    private static synchronized Pair<Integer, BehaviorControl<LivingEntity>> getMinimalLookBehavior() {
         return Pair.of(
             5,
             new RunOne<>(
@@ -317,12 +_,12 @@
         );
     }
 
-    private static boolean raidExistsAndActive(ServerLevel level, LivingEntity body) {
+    private static synchronized boolean raidExistsAndActive(ServerLevel level, LivingEntity body) {
         Raid currentRaid = level.getRaidAt(body.blockPosition());
         return currentRaid != null && currentRaid.isActive() && !currentRaid.isVictory() && !currentRaid.isLoss();
     }
 
-    private static boolean raidExistsAndNotVictory(ServerLevel level, LivingEntity body) {
+    private static synchronized boolean raidExistsAndNotVictory(ServerLevel level, LivingEntity body) {
         Raid currentRaid = level.getRaidAt(body.blockPosition());
         return currentRaid != null && currentRaid.isVictory();
     }
