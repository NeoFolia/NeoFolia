--- a/net/minecraft/world/entity/ai/behavior/GoAndGiveItemsToTarget.java
+++ b/net/minecraft/world/entity/ai/behavior/GoAndGiveItemsToTarget.java
@@ -44,27 +_,27 @@
     }
 
     @Override
-    protected boolean checkExtraStartConditions(ServerLevel level, E body) {
-        return this.canThrowItemToTarget(body);
-    }
-
-    @Override
-    protected boolean canStillUse(ServerLevel level, E body, long timestamp) {
-        return this.canThrowItemToTarget(body);
-    }
-
-    @Override
-    protected void start(ServerLevel level, E body, long timestamp) {
+    protected synchronized boolean checkExtraStartConditions(ServerLevel level, E body) {
+        return this.canThrowItemToTarget(body);
+    }
+
+    @Override
+    protected synchronized boolean canStillUse(ServerLevel level, E body, long timestamp) {
+        return this.canThrowItemToTarget(body);
+    }
+
+    @Override
+    protected synchronized void start(ServerLevel level, E body, long timestamp) {
         this.targetPositionGetter
             .apply(body)
+            .filter(positionTracker -> this.isRegionSafe(body, positionTracker))
             .ifPresent(positionTracker -> BehaviorUtils.setWalkAndLookTargetMemories(body, positionTracker, this.speedModifier, 3));
     }
 
     @Override
-    protected void tick(ServerLevel level, E body, long timestamp) {
+    protected synchronized void tick(ServerLevel level, E body, long timestamp) {
         Optional<PositionTracker> targetPosition = this.targetPositionGetter.apply(body);
-        if (!targetPosition.isEmpty()) {
-            PositionTracker depositTarget = targetPosition.get();
+        targetPosition.filter(depositTarget -> this.isRegionSafe(body, depositTarget)).ifPresent(depositTarget -> {
             Vec3 depositPosition = depositTarget.currentPosition();
             double distanceToTarget = depositPosition.distanceTo(body.getEyePosition());
             if (distanceToTarget < 3.0) {
@@ -75,16 +_,20 @@
                     body.getBrain().setMemory(MemoryModuleType.ITEM_PICKUP_COOLDOWN_TICKS, 60);
                 }
             }
-        }
+        });
     }
 
-    private boolean canThrowItemToTarget(E body) {
+    private synchronized boolean canThrowItemToTarget(E body) {
         if (body.getInventory().isEmpty()) {
             return false;
         } else {
             Optional<PositionTracker> positionTracker = this.targetPositionGetter.apply(body);
-            return positionTracker.isPresent();
+            return positionTracker.filter(tracker -> this.isRegionSafe(body, tracker)).isPresent();
         }
+    }
+
+    private synchronized boolean isRegionSafe(E body, PositionTracker tracker) {
+        return tracker.isRegionSafeFor(body);
     }
 
     @FunctionalInterface
