--- a/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java
+++ b/net/minecraft/world/entity/ai/behavior/VillagerMakeLove.java
@@ -29,16 +_,25 @@
         );
     }
 
-    protected boolean checkExtraStartConditions(ServerLevel level, Villager body) {
+    protected synchronized boolean checkExtraStartConditions(ServerLevel level, Villager body) {
         return this.isBreedingPossible(body);
     }
 
-    protected boolean canStillUse(ServerLevel level, Villager body, long timestamp) {
+    protected synchronized boolean canStillUse(ServerLevel level, Villager body, long timestamp) {
         return timestamp <= this.birthTimestamp && this.isBreedingPossible(body);
     }
 
-    protected void start(ServerLevel level, Villager body, long timestamp) {
-        AgeableMob breedTarget = body.getBrain().getMemory(MemoryModuleType.BREED_TARGET).get();
+    protected synchronized void start(ServerLevel level, Villager body, long timestamp) {
+        Optional<AgeableMob> breedTargetMemory = body.getBrain().getMemory(MemoryModuleType.BREED_TARGET);
+        if (breedTargetMemory.isEmpty()) {
+            return;
+        }
+
+        AgeableMob breedTarget = breedTargetMemory.orElseThrow();
+        if (BehaviorUtils.regionSafeEntity(body, breedTarget) == null) {
+            return;
+        }
+
         BehaviorUtils.lockGazeAndWalkToEachOther(body, breedTarget, 0.5F, 2);
         level.broadcastEntityEvent(breedTarget, (byte)18);
         level.broadcastEntityEvent(body, (byte)18);
@@ -46,8 +_,15 @@
         this.birthTimestamp = timestamp + duration;
     }
 
-    protected void tick(ServerLevel level, Villager body, long timestamp) {
-        Villager target = (Villager)body.getBrain().getMemory(MemoryModuleType.BREED_TARGET).get();
+    protected synchronized void tick(ServerLevel level, Villager body, long timestamp) {
+        Optional<AgeableMob> breedTargetMemory = body.getBrain().getMemory(MemoryModuleType.BREED_TARGET);
+        if (breedTargetMemory.isEmpty() || !(breedTargetMemory.orElseThrow() instanceof Villager target)) {
+            return;
+        }
+        if (BehaviorUtils.regionSafeEntity(body, target) == null) {
+            return;
+        }
+
         if (!(body.distanceToSqr(target) > 5.0)) {
             BehaviorUtils.lockGazeAndWalkToEachOther(body, target, 0.5F, 2);
             if (timestamp >= this.birthTimestamp) {
@@ -61,7 +_,7 @@
         }
     }
 
-    private void tryToGiveBirth(ServerLevel level, Villager body, Villager target) {
+    private synchronized void tryToGiveBirth(ServerLevel level, Villager body, Villager target) {
         Optional<BlockPos> childsBed = this.takeVacantBed(level, body);
         if (childsBed.isEmpty()) {
             level.broadcastEntityEvent(target, (byte)13);
@@ -69,36 +_,41 @@
         } else {
             Optional<Villager> child = this.breed(level, body, target);
             if (child.isPresent()) {
-                this.giveBedToChild(level, child.get(), childsBed.get());
+                this.giveBedToChild(level, child.orElseThrow(), childsBed.orElseThrow());
             } else {
-                level.getPoiManager().release(childsBed.get());
-                level.debugSynchronizers().updatePoi(childsBed.get());
+                BlockPos bedPos = childsBed.orElseThrow();
+                level.getPoiManager().release(bedPos);
+                level.debugSynchronizers().updatePoi(bedPos);
             }
         }
     }
 
-    protected void stop(ServerLevel level, Villager body, long timestamp) {
+    protected synchronized void stop(ServerLevel level, Villager body, long timestamp) {
         body.getBrain().eraseMemory(MemoryModuleType.BREED_TARGET);
     }
 
-    private boolean isBreedingPossible(Villager myBody) {
+    private synchronized boolean isBreedingPossible(Villager myBody) {
         Brain<Villager> brain = myBody.getBrain();
-        Optional<AgeableMob> breedTarget = brain.getMemory(MemoryModuleType.BREED_TARGET).filter(entity -> entity.is(EntityType.VILLAGER));
+        Optional<AgeableMob> breedTarget = brain.getMemory(MemoryModuleType.BREED_TARGET)
+            .filter(entity -> BehaviorUtils.regionSafeEntity(myBody, entity) != null)
+            .filter(entity -> entity.is(EntityType.VILLAGER));
         return breedTarget.isEmpty()
             ? false
-            : BehaviorUtils.targetIsValid(brain, MemoryModuleType.BREED_TARGET, EntityType.VILLAGER) && myBody.canBreed() && breedTarget.get().canBreed();
+            : BehaviorUtils.targetIsValid(myBody, MemoryModuleType.BREED_TARGET, EntityType.VILLAGER)
+                && myBody.canBreed()
+                && breedTarget.map(AgeableMob::canBreed).orElse(false);
     }
 
-    private Optional<BlockPos> takeVacantBed(ServerLevel level, Villager body) {
+    private synchronized Optional<BlockPos> takeVacantBed(ServerLevel level, Villager body) {
         return level.getPoiManager().take(p -> p.is(PoiTypes.HOME), (poiType, poiPos) -> this.canReach(body, poiPos, poiType), body.blockPosition(), 48);
     }
 
-    private boolean canReach(Villager body, BlockPos poiPos, Holder<PoiType> poiType) {
+    private synchronized boolean canReach(Villager body, BlockPos poiPos, Holder<PoiType> poiType) {
         Path path = body.getNavigation().createPath(poiPos, poiType.value().validRange());
         return path != null && path.canReach();
     }
 
-    private Optional<Villager> breed(ServerLevel level, Villager source, Villager target) {
+    private synchronized Optional<Villager> breed(ServerLevel level, Villager source, Villager target) {
         Villager child = source.getBreedOffspring(level, target);
         if (child == null) {
             return Optional.empty();
@@ -108,12 +_,14 @@
             child.setAge(-24000);
             child.snapTo(source.getX(), source.getY(), source.getZ(), 0.0F, 0.0F);
             level.addFreshEntityWithPassengers(child);
+            // Neo: If villager is blocked from spawning (e.g., FinalizeSpawnEvent), then breed should be unsuccessful
+            if (!child.isAddedToLevel()) return Optional.empty();
             level.broadcastEntityEvent(child, (byte)12);
             return Optional.of(child);
         }
     }
 
-    private void giveBedToChild(ServerLevel level, Villager child, BlockPos bedPos) {
+    private synchronized void giveBedToChild(ServerLevel level, Villager child, BlockPos bedPos) {
         GlobalPos globalBedPos = GlobalPos.of(level.dimension(), bedPos);
         child.getBrain().setMemory(MemoryModuleType.HOME, globalBedPos);
     }
