--- a/net/minecraft/world/entity/ai/behavior/TryFindLandNearWater.java
+++ b/net/minecraft/world/entity/ai/behavior/TryFindLandNearWater.java
@@ -7,49 +_,67 @@
 import net.minecraft.world.entity.ai.behavior.declarative.BehaviorBuilder;
 import net.minecraft.world.entity.ai.memory.MemoryModuleType;
 import net.minecraft.world.entity.ai.memory.WalkTarget;
+import net.minecraft.world.level.Level;
 import net.minecraft.world.level.block.Blocks;
 import net.minecraft.world.phys.shapes.CollisionContext;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.apache.commons.lang3.mutable.MutableLong;
 
 public class TryFindLandNearWater {
-    public static BehaviorControl<PathfinderMob> create(int range, float speedModifier) {
+    public static synchronized BehaviorControl<PathfinderMob> create(int range, float speedModifier) {
         MutableLong nextOkStartTime = new MutableLong(0L);
         return BehaviorBuilder.create(
             i -> i.group(i.absent(MemoryModuleType.ATTACK_TARGET), i.absent(MemoryModuleType.WALK_TARGET), i.registered(MemoryModuleType.LOOK_TARGET))
                 .apply(
                     i,
                     (attackTarget, walkTarget, lookTarget) -> (level, body, timestamp) -> {
-                        if (level.getFluidState(body.blockPosition()).is(FluidTags.WATER)) {
-                            return false;
-                        } else if (timestamp < nextOkStartTime.longValue()) {
-                            nextOkStartTime.setValue(timestamp + 40L);
-                            return true;
-                        } else {
-                            CollisionContext context = CollisionContext.of(body);
+                        synchronized (nextOkStartTime) {
                             BlockPos bodyBlockPos = body.blockPosition();
-                            BlockPos.MutableBlockPos testPos = new BlockPos.MutableBlockPos();
-
-                            label45:
-                            for (BlockPos pos : BlockPos.withinManhattan(bodyBlockPos, range, range, range)) {
-                                if ((pos.getX() != bodyBlockPos.getX() || pos.getZ() != bodyBlockPos.getZ())
-                                    && level.getBlockState(pos).getCollisionShape(level, pos, context).isEmpty()
-                                    && !level.getBlockState(testPos.setWithOffset(pos, Direction.DOWN)).getCollisionShape(level, pos, context).isEmpty()) {
-                                    for (Direction direction : Direction.Plane.HORIZONTAL) {
-                                        testPos.setWithOffset(pos, direction);
-                                        if (level.getBlockState(testPos).isAir() && level.getBlockState(testPos.move(Direction.DOWN)).is(Blocks.WATER)) {
-                                            lookTarget.set(new BlockPosTracker(pos));
-                                            walkTarget.set(new WalkTarget(new BlockPosTracker(pos), speedModifier, 0));
-                                            break label45;
+                            if (!canReadBlock(level, bodyBlockPos) || level.getFluidState(bodyBlockPos).is(FluidTags.WATER)) {
+                                return false;
+                            } else if (timestamp < nextOkStartTime.longValue()) {
+                                nextOkStartTime.setValue(timestamp + 40L);
+                                return true;
+                            } else {
+                                CollisionContext context = CollisionContext.of(body);
+                                BlockPos.MutableBlockPos testPos = new BlockPos.MutableBlockPos();
+
+                                label45:
+                                for (BlockPos pos : BlockPos.withinManhattan(bodyBlockPos, range, range, range)) {
+                                    BlockPos below = testPos.setWithOffset(pos, Direction.DOWN).immutable();
+                                    if ((pos.getX() != bodyBlockPos.getX() || pos.getZ() != bodyBlockPos.getZ())
+                                        && canReadBlock(level, pos)
+                                        && canReadBlock(level, below)
+                                        && level.getBlockState(pos).getCollisionShape(level, pos, context).isEmpty()
+                                        && !level.getBlockState(below).getCollisionShape(level, pos, context).isEmpty()) {
+                                        for (Direction direction : Direction.Plane.HORIZONTAL) {
+                                            testPos.setWithOffset(pos, direction);
+                                            BlockPos adjacent = testPos.immutable();
+                                            BlockPos adjacentBelow = testPos.move(Direction.DOWN).immutable();
+                                            if (!canReadBlock(level, adjacent) || !canReadBlock(level, adjacentBelow)) {
+                                                continue;
+                                            }
+
+                                            if (level.getBlockState(adjacent).isAir() && level.getBlockState(adjacentBelow).is(Blocks.WATER)) {
+                                                BlockPos targetPos = pos.immutable();
+                                                lookTarget.set(new BlockPosTracker(targetPos));
+                                                walkTarget.set(new WalkTarget(new BlockPosTracker(targetPos), speedModifier, 0));
+                                                break label45;
+                                            }
                                         }
                                     }
                                 }
+
+                                nextOkStartTime.setValue(timestamp + 40L);
+                                return true;
                             }
-
-                            nextOkStartTime.setValue(timestamp + 40L);
-                            return true;
                         }
                     }
                 )
         );
+    }
+
+    private static boolean canReadBlock(Level level, BlockPos pos) {
+        return RegionizedWorldGuard.shouldLoadChunk(level, pos.getX() >> 4, pos.getZ() >> 4);
     }
 }
