--- a/net/minecraft/world/entity/ai/goal/LandOnOwnersShoulderGoal.java
+++ b/net/minecraft/world/entity/ai/goal/LandOnOwnersShoulderGoal.java
@@ -2,6 +_,8 @@
 
 import net.minecraft.server.level.ServerPlayer;
 import net.minecraft.world.entity.animal.parrot.ShoulderRidingEntity;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
+import org.jspecify.annotations.Nullable;
 
 public class LandOnOwnersShoulderGoal extends Goal {
     private final ShoulderRidingEntity entity;
@@ -12,8 +_,9 @@
     }
 
     @Override
-    public boolean canUse() {
-        if (!(this.entity.getOwner() instanceof ServerPlayer owner)) {
+    public synchronized boolean canUse() {
+        ServerPlayer owner = this.regionSafeOwner(this.entity.getOwner() instanceof ServerPlayer player ? player : null);
+        if (owner == null) {
             return false;
         } else {
             boolean ownerThatCanBeSatOn = !owner.isSpectator() && !owner.getAbilities().flying && !owner.isInWater() && !owner.isInPowderSnow;
@@ -22,21 +_,26 @@
     }
 
     @Override
-    public boolean isInterruptable() {
+    public synchronized boolean isInterruptable() {
         return !this.isSittingOnShoulder;
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.isSittingOnShoulder = false;
     }
 
     @Override
-    public void tick() {
+    public synchronized void tick() {
         if (!this.isSittingOnShoulder && !this.entity.isInSittingPose() && !this.entity.isLeashed()) {
-            if (this.entity.getOwner() instanceof ServerPlayer owner && this.entity.getBoundingBox().intersects(owner.getBoundingBox())) {
+            ServerPlayer owner = this.regionSafeOwner(this.entity.getOwner() instanceof ServerPlayer player ? player : null);
+            if (owner != null && this.entity.getBoundingBox().intersects(owner.getBoundingBox())) {
                 this.isSittingOnShoulder = this.entity.setEntityOnShoulder(owner);
             }
         }
+    }
+
+    private synchronized @Nullable ServerPlayer regionSafeOwner(@Nullable ServerPlayer owner) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.entity.level(), owner);
     }
 }
