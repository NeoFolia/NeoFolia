--- a/net/minecraft/world/entity/ai/goal/OfferFlowerGoal.java
+++ b/net/minecraft/world/entity/ai/goal/OfferFlowerGoal.java
@@ -10,6 +_,7 @@
 import net.minecraft.world.item.Item;
 import net.minecraft.world.item.Items;
 import net.minecraft.world.phys.AABB;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class OfferFlowerGoal extends Goal {
@@ -26,7 +_,7 @@
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         if (!this.golem.level().isBrightOutside()) {
             return false;
         } else if (this.golem.getRandom().nextInt(8000) != 0) {
@@ -42,26 +_,28 @@
                     this.golem.getZ(),
                     this.getGolemBoundingBox()
                 );
+            this.entity = this.regionSafeEntity(this.entity);
             return this.entity != null;
         }
     }
 
     @Override
-    public boolean canContinueToUse() {
-        return this.tick > 0;
+    public synchronized boolean canContinueToUse() {
+        return this.tick > 0 && this.regionSafeEntity(this.entity) != null;
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.tick = this.adjustedTickDelay(400);
         this.golem.offerFlower(true);
     }
 
     @Override
-    public void stop() {
+    public synchronized void stop() {
+        LivingEntity entity = this.regionSafeEntity(this.entity);
         this.golem.offerFlower(false);
         if (this.tick == 0
-            && this.entity instanceof Mob mob
+            && entity instanceof Mob mob
             && mob.is(EntityTypeTags.ACCEPTS_IRON_GOLEM_GIFT)
             && mob.getItemBySlot(CopperGolem.EQUIPMENT_SLOT_ANTENNA).isEmpty()
             && this.getGolemBoundingBox().intersects(mob.getBoundingBox())) {
@@ -70,12 +_,14 @@
         }
 
         this.entity = null;
+        this.tick = 0;
     }
 
     @Override
-    public void tick() {
-        if (this.entity != null) {
-            this.golem.getLookControl().setLookAt(this.entity, 30.0F, 30.0F);
+    public synchronized void tick() {
+        LivingEntity entity = this.regionSafeEntity(this.entity);
+        if (entity != null) {
+            this.golem.getLookControl().setLookAt(entity, 30.0F, 30.0F);
         }
 
         this.tick--;
@@ -83,5 +_,9 @@
 
     private AABB getGolemBoundingBox() {
         return this.golem.getBoundingBox().inflate(6.0, 2.0, 6.0);
+    }
+
+    private synchronized @Nullable LivingEntity regionSafeEntity(@Nullable LivingEntity entity) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.golem.level(), entity);
     }
 }
