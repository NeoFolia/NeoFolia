--- a/net/minecraft/world/entity/ai/goal/TradeWithPlayerGoal.java
+++ b/net/minecraft/world/entity/ai/goal/TradeWithPlayerGoal.java
@@ -3,6 +_,7 @@
 import java.util.EnumSet;
 import net.minecraft.world.entity.npc.villager.AbstractVillager;
 import net.minecraft.world.entity.player.Player;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 
 public class TradeWithPlayerGoal extends Goal {
     private final AbstractVillager mob;
@@ -13,7 +_,7 @@
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         if (!this.mob.isAlive()) {
             return false;
         } else if (this.mob.isInWater()) {
@@ -23,18 +_,18 @@
         } else if (this.mob.hurtMarked) {
             return false;
         } else {
-            Player trader = this.mob.getTradingPlayer();
+            Player trader = RegionizedWorldGuard.restrictEntityToCurrentRegion(this.mob.level(), this.mob.getTradingPlayer());
             return trader == null ? false : !(this.mob.distanceToSqr(trader) > 16.0);
         }
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.mob.getNavigation().stop();
     }
 
     @Override
-    public void stop() {
+    public synchronized void stop() {
         this.mob.setTradingPlayer(null);
     }
 }
