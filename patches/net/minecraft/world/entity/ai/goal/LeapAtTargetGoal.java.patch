--- a/net/minecraft/world/entity/ai/goal/LeapAtTargetGoal.java
+++ b/net/minecraft/world/entity/ai/goal/LeapAtTargetGoal.java
@@ -4,10 +_,12 @@
 import net.minecraft.world.entity.LivingEntity;
 import net.minecraft.world.entity.Mob;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
+import org.jspecify.annotations.Nullable;
 
 public class LeapAtTargetGoal extends Goal {
     private final Mob mob;
-    private LivingEntity target;
+    private @Nullable LivingEntity target;
     private final float yd;
 
     public LeapAtTargetGoal(Mob mob, float yd) {
@@ -17,11 +_,11 @@
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         if (this.mob.hasControllingPassenger()) {
             return false;
         } else {
-            this.target = this.mob.getTarget();
+            this.target = this.regionSafeTarget(this.mob.getTarget());
             if (this.target == null) {
                 return false;
             } else {
@@ -36,18 +_,26 @@
     }
 
     @Override
-    public boolean canContinueToUse() {
+    public synchronized boolean canContinueToUse() {
         return !this.mob.onGround();
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
+        LivingEntity target = this.regionSafeTarget(this.target);
+        if (target == null) {
+            return;
+        }
         Vec3 movement = this.mob.getDeltaMovement();
-        Vec3 delta = new Vec3(this.target.getX() - this.mob.getX(), 0.0, this.target.getZ() - this.mob.getZ());
+        Vec3 delta = new Vec3(target.getX() - this.mob.getX(), 0.0, target.getZ() - this.mob.getZ());
         if (delta.lengthSqr() > 1.0E-7) {
             delta = delta.normalize().scale(0.4).add(movement.scale(0.2));
         }
 
         this.mob.setDeltaMovement(delta.x, this.yd, delta.z);
+    }
+
+    private synchronized @Nullable LivingEntity regionSafeTarget(@Nullable LivingEntity target) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.mob.level(), target);
     }
 }
