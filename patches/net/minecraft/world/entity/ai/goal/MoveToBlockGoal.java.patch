--- a/net/minecraft/world/entity/ai/goal/MoveToBlockGoal.java
+++ b/net/minecraft/world/entity/ai/goal/MoveToBlockGoal.java
@@ -4,6 +_,7 @@
 import net.minecraft.core.BlockPos;
 import net.minecraft.world.entity.PathfinderMob;
 import net.minecraft.world.level.LevelReader;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 
 public abstract class MoveToBlockGoal extends Goal {
     private static final int GIVE_UP_TICKS = 1200;
@@ -34,7 +_,7 @@
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         if (this.nextStartTick > 0) {
             this.nextStartTick--;
             return false;
@@ -44,41 +_,44 @@
         }
     }
 
-    protected int nextStartTick(PathfinderMob mob) {
+    protected synchronized int nextStartTick(PathfinderMob mob) {
         return reducedTickDelay(200 + mob.getRandom().nextInt(200));
     }
 
     @Override
-    public boolean canContinueToUse() {
-        return this.tryTicks >= -this.maxStayTicks && this.tryTicks <= 1200 && this.isValidTarget(this.mob.level(), this.blockPos);
+    public synchronized boolean canContinueToUse() {
+        return this.tryTicks >= -this.maxStayTicks
+            && this.tryTicks <= 1200
+            && this.canReadBlock(this.blockPos)
+            && this.isValidTarget(this.mob.level(), this.blockPos);
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.moveMobToBlock();
         this.tryTicks = 0;
         this.maxStayTicks = this.mob.getRandom().nextInt(this.mob.getRandom().nextInt(1200) + 1200) + 1200;
     }
 
-    protected void moveMobToBlock() {
+    protected synchronized void moveMobToBlock() {
         this.mob.getNavigation().moveTo(this.blockPos.getX() + 0.5, this.blockPos.getY() + 1, this.blockPos.getZ() + 0.5, this.speedModifier);
     }
 
-    public double acceptedDistance() {
+    public synchronized double acceptedDistance() {
         return 1.0;
     }
 
-    protected BlockPos getMoveToTarget() {
+    protected synchronized BlockPos getMoveToTarget() {
         return this.blockPos.above();
     }
 
     @Override
-    public boolean requiresUpdateEveryTick() {
+    public synchronized boolean requiresUpdateEveryTick() {
         return true;
     }
 
     @Override
-    public void tick() {
+    public synchronized void tick() {
         BlockPos moveToTarget = this.getMoveToTarget();
         if (!moveToTarget.closerToCenterThan(this.mob.position(), this.acceptedDistance())) {
             this.reachedTarget = false;
@@ -92,15 +_,15 @@
         }
     }
 
-    public boolean shouldRecalculatePath() {
+    public synchronized boolean shouldRecalculatePath() {
         return this.tryTicks % 40 == 0;
     }
 
-    protected boolean isReachedTarget() {
+    protected synchronized boolean isReachedTarget() {
         return this.reachedTarget;
     }
 
-    protected boolean findNearestBlock() {
+    protected synchronized boolean findNearestBlock() {
         int horizontalSearch = this.searchRange;
         int verticalSearch = this.verticalSearchRange;
         BlockPos mobPos = this.mob.blockPosition();
@@ -111,8 +_,8 @@
                 for (int x = 0; x <= r; x = x > 0 ? -x : 1 - x) {
                     for (int z = x < r && x > -r ? r : 0; z <= r; z = z > 0 ? -z : 1 - z) {
                         pos.setWithOffset(mobPos, x, y - 1, z);
-                        if (this.mob.isWithinHome(pos) && this.isValidTarget(this.mob.level(), pos)) {
-                            this.blockPos = pos;
+                        if (this.mob.isWithinHome(pos) && this.canReadBlock(pos) && this.isValidTarget(this.mob.level(), pos)) {
+                            this.blockPos = pos.immutable();
                             return true;
                         }
                     }
@@ -124,4 +_,8 @@
     }
 
     protected abstract boolean isValidTarget(LevelReader level, BlockPos pos);
+
+    protected synchronized boolean canReadBlock(BlockPos pos) {
+        return RegionizedWorldGuard.shouldLoadChunk(this.mob.level(), pos.getX() >> 4, pos.getZ() >> 4);
+    }
 }
