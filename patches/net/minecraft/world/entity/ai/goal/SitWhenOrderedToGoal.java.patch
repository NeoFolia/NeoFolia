--- a/net/minecraft/world/entity/ai/goal/SitWhenOrderedToGoal.java
+++ b/net/minecraft/world/entity/ai/goal/SitWhenOrderedToGoal.java
@@ -3,6 +_,8 @@
 import java.util.EnumSet;
 import net.minecraft.world.entity.LivingEntity;
 import net.minecraft.world.entity.TamableAnimal;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
+import org.jspecify.annotations.Nullable;
 
 public class SitWhenOrderedToGoal extends Goal {
     private final TamableAnimal mob;
@@ -13,12 +_,12 @@
     }
 
     @Override
-    public boolean canContinueToUse() {
+    public synchronized boolean canContinueToUse() {
         return this.mob.isOrderedToSit();
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         boolean orderedToSit = this.mob.isOrderedToSit();
         if (!orderedToSit && !this.mob.isTame()) {
             return false;
@@ -27,23 +_,28 @@
         } else if (!this.mob.onGround()) {
             return false;
         } else {
-            LivingEntity owner = this.mob.getOwner();
+            LivingEntity owner = this.regionSafeOwner(this.mob.getOwner());
             if (owner == null || owner.level() != this.mob.level()) {
                 return true;
             } else {
-                return this.mob.distanceToSqr(owner) < 144.0 && owner.getLastHurtByMob() != null ? false : orderedToSit;
+                LivingEntity ownerLastHurtBy = this.regionSafeOwner(owner.getLastHurtByMob());
+                return this.mob.distanceToSqr(owner) < 144.0 && ownerLastHurtBy != null ? false : orderedToSit;
             }
         }
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.mob.getNavigation().stop();
         this.mob.setInSittingPose(true);
     }
 
     @Override
-    public void stop() {
+    public synchronized void stop() {
         this.mob.setInSittingPose(false);
+    }
+
+    private synchronized @Nullable LivingEntity regionSafeOwner(@Nullable LivingEntity owner) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.mob.level(), owner);
     }
 }
