--- a/net/minecraft/world/entity/ai/goal/FleeSunGoal.java
+++ b/net/minecraft/world/entity/ai/goal/FleeSunGoal.java
@@ -3,10 +_,12 @@
 import java.util.EnumSet;
 import net.minecraft.core.BlockPos;
 import net.minecraft.util.RandomSource;
+import net.minecraft.world.entity.LivingEntity;
 import net.minecraft.world.entity.EquipmentSlot;
 import net.minecraft.world.entity.PathfinderMob;
 import net.minecraft.world.level.Level;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class FleeSunGoal extends Goal {
@@ -25,21 +_,21 @@
     }
 
     @Override
-    public boolean canUse() {
-        if (this.mob.getTarget() != null) {
+    public synchronized boolean canUse() {
+        if (this.regionSafeTarget(this.mob.getTarget()) != null) {
             return false;
         } else if (!this.level.isBrightOutside()) {
             return false;
         } else if (!this.mob.isOnFire()) {
             return false;
-        } else if (!this.level.canSeeSky(this.mob.blockPosition())) {
+        } else if (!this.canReadBlock(this.mob.blockPosition()) || !this.level.canSeeSky(this.mob.blockPosition())) {
             return false;
         } else {
             return !this.mob.getItemBySlot(EquipmentSlot.HEAD).isEmpty() ? false : this.setWantedPos();
         }
     }
 
-    protected boolean setWantedPos() {
+    protected synchronized boolean setWantedPos() {
         Vec3 pos = this.getHidePos();
         if (pos == null) {
             return false;
@@ -52,26 +_,34 @@
     }
 
     @Override
-    public boolean canContinueToUse() {
+    public synchronized boolean canContinueToUse() {
         return !this.mob.getNavigation().isDone();
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         this.mob.getNavigation().moveTo(this.wantedX, this.wantedY, this.wantedZ, this.speedModifier);
     }
 
-    protected @Nullable Vec3 getHidePos() {
+    protected synchronized @Nullable Vec3 getHidePos() {
         RandomSource random = this.mob.getRandom();
         BlockPos pos = this.mob.blockPosition();
 
         for (int i = 0; i < 10; i++) {
             BlockPos randomPos = pos.offset(random.nextInt(20) - 10, random.nextInt(6) - 3, random.nextInt(20) - 10);
-            if (!this.level.canSeeSky(randomPos) && this.mob.getWalkTargetValue(randomPos) < 0.0F) {
+            if (this.canReadBlock(randomPos) && !this.level.canSeeSky(randomPos) && this.mob.getWalkTargetValue(randomPos) < 0.0F) {
                 return Vec3.atBottomCenterOf(randomPos);
             }
         }
 
         return null;
+    }
+
+    private synchronized @Nullable LivingEntity regionSafeTarget(@Nullable LivingEntity target) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(this.mob.level(), target);
+    }
+
+    private synchronized boolean canReadBlock(BlockPos pos) {
+        return RegionizedWorldGuard.shouldLoadChunk(this.level, pos.getX() >> 4, pos.getZ() >> 4);
     }
 }
