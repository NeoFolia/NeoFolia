--- a/net/minecraft/world/entity/ai/goal/WrappedGoal.java
+++ b/net/minecraft/world/entity/ai/goal/WrappedGoal.java
@@ -13,27 +_,27 @@
         this.goal = goal;
     }
 
-    public boolean canBeReplacedBy(WrappedGoal goal) {
+    public synchronized boolean canBeReplacedBy(WrappedGoal goal) {
         return this.isInterruptable() && goal.getPriority() < this.getPriority();
     }
 
     @Override
-    public boolean canUse() {
+    public synchronized boolean canUse() {
         return this.goal.canUse();
     }
 
     @Override
-    public boolean canContinueToUse() {
+    public synchronized boolean canContinueToUse() {
         return this.goal.canContinueToUse();
     }
 
     @Override
-    public boolean isInterruptable() {
+    public synchronized boolean isInterruptable() {
         return this.goal.isInterruptable();
     }
 
     @Override
-    public void start() {
+    public synchronized void start() {
         if (!this.isRunning) {
             this.isRunning = true;
             this.goal.start();
@@ -41,7 +_,7 @@
     }
 
     @Override
-    public void stop() {
+    public synchronized void stop() {
         if (this.isRunning) {
             this.isRunning = false;
             this.goal.stop();
@@ -49,44 +_,44 @@
     }
 
     @Override
-    public boolean requiresUpdateEveryTick() {
+    public synchronized boolean requiresUpdateEveryTick() {
         return this.goal.requiresUpdateEveryTick();
     }
 
     @Override
-    protected int adjustedTickDelay(int ticks) {
+    protected synchronized int adjustedTickDelay(int ticks) {
         return this.goal.adjustedTickDelay(ticks);
     }
 
     @Override
-    public void tick() {
+    public synchronized void tick() {
         this.goal.tick();
     }
 
     @Override
-    public void setFlags(EnumSet<Goal.Flag> requiredControlFlags) {
+    public synchronized void setFlags(EnumSet<Goal.Flag> requiredControlFlags) {
         this.goal.setFlags(requiredControlFlags);
     }
 
     @Override
-    public EnumSet<Goal.Flag> getFlags() {
+    public synchronized EnumSet<Goal.Flag> getFlags() {
         return this.goal.getFlags();
     }
 
-    public boolean isRunning() {
+    public synchronized boolean isRunning() {
         return this.isRunning;
     }
 
-    public int getPriority() {
+    public synchronized int getPriority() {
         return this.priority;
     }
 
-    public Goal getGoal() {
+    public synchronized Goal getGoal() {
         return this.goal;
     }
 
     @Override
-    public boolean equals(@Nullable Object o) {
+    public synchronized boolean equals(@Nullable Object o) {
         if (this == o) {
             return true;
         } else {
@@ -95,7 +_,7 @@
     }
 
     @Override
-    public int hashCode() {
+    public synchronized int hashCode() {
         return this.goal.hashCode();
     }
 }
