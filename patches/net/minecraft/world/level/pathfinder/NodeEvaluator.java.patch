--- a/net/minecraft/world/level/pathfinder/NodeEvaluator.java
+++ b/net/minecraft/world/level/pathfinder/NodeEvaluator.java
@@ -23,7 +_,7 @@
     protected boolean canFloat;
     protected boolean canWalkOverFences;
 
-    public void prepare(PathNavigationRegion level, Mob entity) {
+    public synchronized void prepare(PathNavigationRegion level, Mob entity) {
         this.currentContext = new PathfindingContext(level, entity);
         this.mob = entity;
         this.nodes.clear();
@@ -32,16 +_,16 @@
         this.entityDepth = Mth.floor(entity.getBbWidth() + 1.0F);
     }
 
-    public void done() {
+    public synchronized void done() {
         this.currentContext = null;
         this.mob = null;
     }
 
-    protected Node getNode(BlockPos pos) {
+    protected synchronized Node getNode(BlockPos pos) {
         return this.getNode(pos.getX(), pos.getY(), pos.getZ());
     }
 
-    protected Node getNode(int x, int y, int z) {
+    protected synchronized Node getNode(int x, int y, int z) {
         return this.nodes.computeIfAbsent(Node.createHash(x, y, z), k -> new Node(x, y, z));
     }
 
@@ -49,7 +_,7 @@
 
     public abstract Target getTarget(double x, double y, double z);
 
-    protected Target getTargetNodeAt(double x, double y, double z) {
+    protected synchronized Target getTargetNodeAt(double x, double y, double z) {
         return new Target(this.getNode(Mth.floor(x), Mth.floor(y), Mth.floor(z)));
     }
 
@@ -59,39 +_,39 @@
 
     public abstract PathType getPathType(PathfindingContext context, int x, int y, int z);
 
-    public PathType getPathType(Mob mob, BlockPos pos) {
+    public synchronized PathType getPathType(Mob mob, BlockPos pos) {
         return this.getPathType(new PathfindingContext(mob.level(), mob), pos.getX(), pos.getY(), pos.getZ());
     }
 
-    public void setCanPassDoors(boolean canPassDoors) {
+    public synchronized void setCanPassDoors(boolean canPassDoors) {
         this.canPassDoors = canPassDoors;
     }
 
-    public void setCanOpenDoors(boolean canOpenDoors) {
+    public synchronized void setCanOpenDoors(boolean canOpenDoors) {
         this.canOpenDoors = canOpenDoors;
     }
 
-    public void setCanFloat(boolean canFloat) {
+    public synchronized void setCanFloat(boolean canFloat) {
         this.canFloat = canFloat;
     }
 
-    public void setCanWalkOverFences(boolean canWalkOverFences) {
+    public synchronized void setCanWalkOverFences(boolean canWalkOverFences) {
         this.canWalkOverFences = canWalkOverFences;
     }
 
-    public boolean canPassDoors() {
+    public synchronized boolean canPassDoors() {
         return this.canPassDoors;
     }
 
-    public boolean canOpenDoors() {
+    public synchronized boolean canOpenDoors() {
         return this.canOpenDoors;
     }
 
-    public boolean canFloat() {
+    public synchronized boolean canFloat() {
         return this.canFloat;
     }
 
-    public boolean canWalkOverFences() {
+    public synchronized boolean canWalkOverFences() {
         return this.canWalkOverFences;
     }
 
