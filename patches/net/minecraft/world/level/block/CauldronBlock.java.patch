--- a/net/minecraft/world/level/block/CauldronBlock.java
+++ b/net/minecraft/world/level/block/CauldronBlock.java
@@ -3,6 +_,8 @@
 import com.mojang.serialization.MapCodec;
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.cauldron.CauldronInteractions;
+import net.minecraft.server.level.ServerLevel;
+import net.minecraft.util.RandomSource;
 import net.minecraft.world.level.Level;
 import net.minecraft.world.level.biome.Biome;
 import net.minecraft.world.level.block.state.BlockBehaviour;
@@ -10,6 +_,7 @@
 import net.minecraft.world.level.gameevent.GameEvent;
 import net.minecraft.world.level.material.Fluid;
 import net.minecraft.world.level.material.Fluids;
+import net.neoforged.neoforge.server.region.RegionizedTickContext;
 
 public class CauldronBlock extends AbstractCauldronBlock {
     public static final MapCodec<CauldronBlock> CODEC = simpleCodec(CauldronBlock::new);
@@ -31,11 +_,16 @@
     }
 
     protected static boolean shouldHandlePrecipitation(Level level, Biome.Precipitation precipitation) {
+        RandomSource random = cauldronRandom(level);
         if (precipitation == Biome.Precipitation.RAIN) {
-            return level.getRandom().nextFloat() < 0.05F;
+            return random.nextFloat() < 0.05F;
         } else {
-            return precipitation == Biome.Precipitation.SNOW ? level.getRandom().nextFloat() < 0.1F : false;
+            return precipitation == Biome.Precipitation.SNOW ? random.nextFloat() < 0.1F : false;
         }
+    }
+
+    private static RandomSource cauldronRandom(Level level) {
+        return level instanceof ServerLevel ? RegionizedTickContext.currentRandom().orElse(level.getRandom()) : level.getRandom();
     }
 
     @Override
@@ -58,6 +_,7 @@
 
     @Override
     protected void receiveStalactiteDrip(BlockState state, Level level, BlockPos pos, Fluid fluid) {
+        if (fluid.getFluidType().handleCauldronDrip(fluid, level, pos)) return;
         if (fluid == Fluids.WATER) {
             BlockState newState = Blocks.WATER_CAULDRON.defaultBlockState();
             level.setBlockAndUpdate(pos, newState);
