--- a/net/minecraft/world/level/block/entity/CrafterBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/CrafterBlockEntity.java
@@ -48,15 +_,19 @@
 
         @Override
         public int get(int dataId) {
-            return dataId == 9 ? this.triggered : this.slotStates[dataId];
+            synchronized (CrafterBlockEntity.this) {
+                return dataId == 9 ? this.triggered : this.slotStates[dataId];
+            }
         }
 
         @Override
         public void set(int dataId, int value) {
-            if (dataId == 9) {
-                this.triggered = value;
-            } else {
-                this.slotStates[dataId] = value;
+            synchronized (CrafterBlockEntity.this) {
+                if (dataId == 9) {
+                    this.triggered = value;
+                } else {
+                    this.slotStates[dataId] = value;
+                }
             }
         }
 
@@ -80,19 +_,19 @@
         return new CrafterMenu(containerId, inventory, this, this.containerData);
     }
 
-    public void setSlotState(int slotId, boolean enabled) {
+    public synchronized void setSlotState(int slotId, boolean enabled) {
         if (this.slotCanBeDisabled(slotId)) {
             this.containerData.set(slotId, enabled ? 0 : 1);
             this.setChanged();
         }
     }
 
-    public boolean isSlotDisabled(int slotId) {
+    public synchronized boolean isSlotDisabled(int slotId) {
         return slotId >= 0 && slotId < 9 ? this.containerData.get(slotId) == 1 : false;
     }
 
     @Override
-    public boolean canPlaceItem(int slot, ItemStack itemStack) {
+    public synchronized boolean canPlaceItem(int slot, ItemStack itemStack) {
         if (this.containerData.get(slot) == 1) {
             return false;
         } else {
@@ -106,7 +_,7 @@
         }
     }
 
-    private boolean smallerStackExist(int baseSize, ItemStack baseItem, int baseSlot) {
+    private synchronized boolean smallerStackExist(int baseSize, ItemStack baseItem, int baseSlot) {
         for (int i = baseSlot + 1; i < 9; i++) {
             if (!this.isSlotDisabled(i)) {
                 ItemStack slotStack = this.getItem(i);
@@ -120,7 +_,7 @@
     }
 
     @Override
-    protected void loadAdditional(ValueInput input) {
+    protected synchronized void loadAdditional(ValueInput input) {
         super.loadAdditional(input);
         this.craftingTicksRemaining = input.getIntOr("crafting_ticks_remaining", 0);
         this.items = NonNullList.withSize(this.getContainerSize(), ItemStack.EMPTY);
@@ -143,7 +_,7 @@
     }
 
     @Override
-    protected void saveAdditional(ValueOutput output) {
+    protected synchronized void saveAdditional(ValueOutput output) {
         super.saveAdditional(output);
         output.putInt("crafting_ticks_remaining", this.craftingTicksRemaining);
         if (!this.trySaveLootTable(output)) {
@@ -155,12 +_,12 @@
     }
 
     @Override
-    public int getContainerSize() {
+    public synchronized int getContainerSize() {
         return 9;
     }
 
     @Override
-    public boolean isEmpty() {
+    public synchronized boolean isEmpty() {
         for (ItemStack is : this.items) {
             if (!is.isEmpty()) {
                 return false;
@@ -171,12 +_,12 @@
     }
 
     @Override
-    public ItemStack getItem(int slot) {
+    public synchronized ItemStack getItem(int slot) {
         return this.items.get(slot);
     }
 
     @Override
-    public void setItem(int slot, ItemStack itemStack) {
+    public synchronized void setItem(int slot, ItemStack itemStack) {
         if (this.isSlotDisabled(slot)) {
             this.setSlotState(slot, true);
         }
@@ -190,12 +_,12 @@
     }
 
     @Override
-    public NonNullList<ItemStack> getItems() {
+    public synchronized NonNullList<ItemStack> getItems() {
         return this.items;
     }
 
     @Override
-    protected void setItems(NonNullList<ItemStack> items) {
+    protected synchronized void setItems(NonNullList<ItemStack> items) {
         this.items = items;
     }
 
@@ -210,13 +_,13 @@
     }
 
     @Override
-    public void fillStackedContents(StackedItemContents contents) {
+    public synchronized void fillStackedContents(StackedItemContents contents) {
         for (ItemStack itemStack : this.items) {
             contents.accountSimpleStack(itemStack);
         }
     }
 
-    private void addDisabledSlots(ValueOutput output) {
+    private synchronized void addDisabledSlots(ValueOutput output) {
         IntList disabledSlots = new IntArrayList();
 
         for (int i = 0; i < 9; i++) {
@@ -228,34 +_,39 @@
         output.putIntArray("disabled_slots", disabledSlots.toIntArray());
     }
 
-    private void addTriggered(ValueOutput output) {
+    private synchronized void addTriggered(ValueOutput output) {
         output.putInt("triggered", this.containerData.get(9));
     }
 
-    public void setTriggered(boolean value) {
+    public synchronized void setTriggered(boolean value) {
         this.containerData.set(9, value ? 1 : 0);
     }
 
     @VisibleForTesting
-    public boolean isTriggered() {
+    public synchronized boolean isTriggered() {
         return this.containerData.get(9) == 1;
     }
 
     public static void serverTick(Level level, BlockPos blockPos, BlockState blockState, CrafterBlockEntity entity) {
-        int craftingTicksRemaining = entity.craftingTicksRemaining - 1;
-        if (craftingTicksRemaining >= 0) {
-            entity.craftingTicksRemaining = craftingTicksRemaining;
-            if (craftingTicksRemaining == 0) {
-                level.setBlock(blockPos, blockState.setValue(CrafterBlock.CRAFTING, false), 3);
+        boolean stopCrafting = false;
+        synchronized (entity) {
+            int craftingTicksRemaining = entity.craftingTicksRemaining - 1;
+            if (craftingTicksRemaining >= 0) {
+                entity.craftingTicksRemaining = craftingTicksRemaining;
+                stopCrafting = craftingTicksRemaining == 0;
             }
         }
+
+        if (stopCrafting) {
+            level.setBlock(blockPos, blockState.setValue(CrafterBlock.CRAFTING, false), 3);
+        }
     }
 
-    public void setCraftingTicksRemaining(int maxCraftingTicks) {
+    public synchronized void setCraftingTicksRemaining(int maxCraftingTicks) {
         this.craftingTicksRemaining = maxCraftingTicks;
     }
 
-    public int getRedstoneSignal() {
+    public synchronized int getRedstoneSignal() {
         int count = 0;
 
         for (int i = 0; i < this.getContainerSize(); i++) {
@@ -268,7 +_,7 @@
         return count;
     }
 
-    private boolean slotCanBeDisabled(int slotId) {
+    private synchronized boolean slotCanBeDisabled(int slotId) {
         return slotId > -1 && slotId < 9 && this.items.get(slotId).isEmpty();
     }
 }
