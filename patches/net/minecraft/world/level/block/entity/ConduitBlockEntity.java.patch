--- a/net/minecraft/world/level/block/entity/ConduitBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/ConduitBlockEntity.java
@@ -30,6 +_,8 @@
 import net.minecraft.world.level.storage.ValueOutput;
 import net.minecraft.world.phys.AABB;
 import net.minecraft.world.phys.Vec3;
+import net.neoforged.neoforge.server.region.RegionizedTickContext;
+import net.neoforged.neoforge.server.region.RegionizedWorldGuard;
 import org.jspecify.annotations.Nullable;
 
 public class ConduitBlockEntity extends BlockEntity {
@@ -114,7 +_,8 @@
             }
 
             if (gameTime > entity.nextAmbientSoundActivation) {
-                entity.nextAmbientSoundActivation = gameTime + 60L + level.getRandom().nextInt(40);
+                RandomSource random = level instanceof ServerLevel serverLevel ? conduitRandom(serverLevel) : level.getRandom();
+                entity.nextAmbientSoundActivation = gameTime + 60L + random.nextInt(40);
                 level.playSound(null, pos, SoundEvents.CONDUIT_AMBIENT_SHORT, SoundSource.BLOCKS, 1.0F, 1.0F);
             }
         }
@@ -131,7 +_,7 @@
             for (int oy = -1; oy <= 1; oy++) {
                 for (int oz = -1; oz <= 1; oz++) {
                     BlockPos testPos = worldPosition.offset(ox, oy, oz);
-                    if (!level.isWaterAt(testPos)) {
+                    if (!canReadBlock(level, testPos) || !level.isWaterAt(testPos)) {
                         return false;
                     }
                 }
@@ -147,12 +_,14 @@
                     if ((ax > 1 || ay > 1 || az > 1)
                         && (ox == 0 && (ay == 2 || az == 2) || oy == 0 && (ax == 2 || az == 2) || ozx == 0 && (ax == 2 || ay == 2))) {
                         BlockPos testPos = worldPosition.offset(ox, oy, ozx);
+                        if (!canReadBlock(level, testPos)) {
+                            continue;
+                        }
+
                         BlockState testBlock = level.getBlockState(testPos);
 
-                        for (Block type : VALID_BLOCKS) {
-                            if (testBlock.is(type)) {
-                                effectBlocks.add(testPos);
-                            }
+                        if (testBlock.isConduitFrame(level, testPos, worldPosition)) {
+                            effectBlocks.add(testPos);
                         }
                     }
                 }
@@ -162,6 +_,10 @@
         return effectBlocks.size() >= 16;
     }
 
+    private static boolean canReadBlock(Level level, BlockPos pos) {
+        return RegionizedWorldGuard.shouldLoadChunk(level, pos.getX() >> 4, pos.getZ() >> 4);
+    }
+
     private static void applyEffects(Level level, BlockPos worldPosition, List<BlockPos> effectBlocks) {
         int activeSize = effectBlocks.size();
         int effectRange = activeSize / 7 * 16;
@@ -169,7 +_,8 @@
         int y = worldPosition.getY();
         int z = worldPosition.getZ();
         AABB bb = new AABB(x, y, z, x + 1, y + 1, z + 1).inflate(effectRange).expandTowards(0.0, level.getHeight(), 0.0);
-        List<Player> players = level.getEntitiesOfClass(Player.class, bb);
+        AABB regionBox = RegionizedWorldGuard.restrictToCurrentRegion(level, bb);
+        List<Player> players = regionBox == null ? List.of() : level.getEntitiesOfClass(Player.class, regionBox);
         if (!players.isEmpty()) {
             for (Player player : players) {
                 if (worldPosition.closerThan(player.blockPosition(), effectRange) && player.isInWaterOrRain()) {
@@ -181,7 +_,7 @@
 
     private static void updateAndAttackTarget(ServerLevel level, BlockPos worldPosition, BlockState blockState, ConduitBlockEntity entity, boolean isActive) {
         EntityReference<LivingEntity> newDestroyTarget = updateDestroyTarget(entity.destroyTarget, level, worldPosition, isActive);
-        LivingEntity targetEntity = EntityReference.getLivingEntity(newDestroyTarget, level);
+        LivingEntity targetEntity = getReadableDestroyTarget(newDestroyTarget, level);
         if (targetEntity != null) {
             level.playSound(
                 null, targetEntity.getX(), targetEntity.getY(), targetEntity.getZ(), SoundEvents.CONDUIT_ATTACK_TARGET, SoundSource.BLOCKS, 1.0F, 1.0F
@@ -203,16 +_,34 @@
         } else if (target == null) {
             return selectNewTarget(level, pos);
         } else {
-            LivingEntity targetEntity = EntityReference.getLivingEntity(target, level);
-            return targetEntity != null && targetEntity.isAlive() && pos.closerThan(targetEntity.blockPosition(), 8.0) ? target : null;
+            LivingEntity targetEntity = getReadableDestroyTarget(target, level);
+            if (targetEntity != null && targetEntity.isAlive() && pos.closerThan(targetEntity.blockPosition(), 8.0)) {
+                return target;
+            }
+
+            return targetExistsOutsideCurrentRegion(target, level) ? target : null;
         }
+    }
+
+    private static @Nullable LivingEntity getReadableDestroyTarget(@Nullable EntityReference<LivingEntity> target, ServerLevel level) {
+        return RegionizedWorldGuard.restrictEntityToCurrentRegion(level, EntityReference.getLivingEntity(target, level));
+    }
+
+    private static boolean targetExistsOutsideCurrentRegion(EntityReference<LivingEntity> target, ServerLevel level) {
+        Entity entity = level.getEntities().get(target.getUUID());
+        return entity != null && RegionizedWorldGuard.restrictEntityToCurrentRegion(level, entity) == null;
     }
 
     private static @Nullable EntityReference<LivingEntity> selectNewTarget(ServerLevel level, BlockPos pos) {
-        List<LivingEntity> candidates = level.getEntitiesOfClass(
-            LivingEntity.class, getDestroyRangeAABB(pos), input -> input instanceof Enemy && input.isInWaterOrRain()
-        );
-        return candidates.isEmpty() ? null : EntityReference.of(Util.getRandom(candidates, level.getRandom()));
+        AABB regionBox = RegionizedWorldGuard.restrictToCurrentRegion(level, getDestroyRangeAABB(pos));
+        List<LivingEntity> candidates = regionBox == null
+            ? List.of()
+            : level.getEntitiesOfClass(LivingEntity.class, regionBox, input -> input instanceof Enemy && input.isInWaterOrRain());
+        return candidates.isEmpty() ? null : EntityReference.of(Util.getRandom(candidates, conduitRandom(level)));
+    }
+
+    private static RandomSource conduitRandom(ServerLevel level) {
+        return RegionizedTickContext.currentRandom().orElse(level.getRandom());
     }
 
     private static AABB getDestroyRangeAABB(BlockPos worldPosition) {
