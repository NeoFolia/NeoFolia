--- a/net/minecraft/world/level/saveddata/WanderingTraderData.java
+++ b/net/minecraft/world/level/saveddata/WanderingTraderData.java
@@ -8,8 +_,8 @@
 public class WanderingTraderData extends SavedData {
     public static final Codec<WanderingTraderData> CODEC = RecordCodecBuilder.create(
         i -> i.group(
-                Codec.INT.optionalFieldOf("spawn_delay", 24000).forGetter(data -> data.spawnDelay),
-                Codec.INT.optionalFieldOf("spawn_chance", 25).forGetter(data -> data.spawnChance)
+                Codec.INT.optionalFieldOf("spawn_delay", 24000).forGetter(WanderingTraderData::spawnDelay),
+                Codec.INT.optionalFieldOf("spawn_chance", 25).forGetter(WanderingTraderData::spawnChance)
             )
             .apply(i, WanderingTraderData::new)
     );
@@ -28,22 +_,22 @@
         this.spawnChance = spawnChance;
     }
 
-    public int spawnDelay() {
+    public synchronized int spawnDelay() {
         return this.spawnDelay;
     }
 
-    public void setSpawnDelay(int spawnDelay) {
+    public synchronized void setSpawnDelay(int spawnDelay) {
         if (this.spawnDelay != spawnDelay) {
             this.spawnDelay = spawnDelay;
             this.setDirty(true);
         }
     }
 
-    public int spawnChance() {
+    public synchronized int spawnChance() {
         return this.spawnChance;
     }
 
-    public void setSpawnChance(int spawnChance) {
+    public synchronized void setSpawnChance(int spawnChance) {
         if (this.spawnChance != spawnChance) {
             this.spawnChance = spawnChance;
             this.setDirty(true);
